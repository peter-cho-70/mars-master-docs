# 개발 현황 (STATUS)

**기준일**: 2026-07-23 · **다음 갱신 예정**: 다음 세션 마감 시
이 문서는 "지금 어디까지 왔나"의 저장소 내 공식 기록입니다. 날짜 기준으로 갱신합니다.

---

## ✅ 완료 — 실장비 검증까지 끝난 것

| 영역 | 내용 | 검증 |
|---|---|---|
| 뉴스 큐시트 파이프라인 | MARS 제작 → Master 진행, 파일/postMessage 핸드오프, 연속 핸드오프 | 실vMix |
| 전환효과 | 타입별 원칙(REPORT=Cut/Cut, ANCHOR=Cut/Mix 등) + 7종 프리셋 전부 | 실vMix |
| 자막(CG) | 8슬롯 오버레이, IN/OUT 타이밍, GT 필드 매칭, 브라우저 오버레이(vMix Browser 입력 + OBS 브라우저 소스 양쪽) | 실vMix·실OBS |
| 서버/클립 재생 | SRV1~3 리스트 재생, 프리롤·조기전환·프레임 트림, 충돌 방지 | 실vMix |
| OBS 병렬 버전 | 씬 전환·클립·CG — 로케일/렌더링 버그 발견·수정 포함 | 실OBS |
| Videohub 라우터 | server.py TCP 릴레이, 아이템별 크로스포인트 전환 (확장 Phase 1) | 실장비 |
| 스위처 | PGM/PVW/AUTO/CUT + 버스 스왑 동기화(07-10 수정), 단축키·팝업 | 실vMix |
| 운영 안전장치 | 30건 UX 감사(가짜 성공 토스트, confirm 게이트, undo 등) 전건 수정 | Playwright |
| 출연(스튜디오 게스트) LIVE 템플릿 (07-13) | MARS `출` 코드와 `중`(중계)을 Master LIVE 시퀀스 엔진에서 분리(`liveSeq.kind`) — 출연은 앵커→앵커갈라치기(DVE2)→기자→기자서버갈라치기(DVE1)→…→Full Shot 마무리로 별도 라벨·구조 진행. MARS 아이템 편집창에 CAM/SRV/DVE 시퀀스 소스 사전 설정 UI 추가(Master 초기값, 방송 중엔 Master가 최종 결정권) | Playwright(MARS→Master 핸드오프·재전송 보존까지 검증) |
| 전환효과 필수 프리셋 강제 + 아이템 더블클릭 편집 (07-15) | Cut/Mix(디졸브)/Fade/Wipe는 vMix와 실연동되는 필수 기본값이라 삭제를 아예 막고, 저장된 프리셋 목록에서 하나라도 빠지면 다음 로드 때 자동 복구(자기 치유)하도록 강화 — 예전엔 삭제 시 확인창만 띄우고 실제로는 지워져 신규 아이템 전환In/Out에 Cut이 안 보이는 상태가 될 수 있었음. 큐시트 아이템(뉴스 큐시트)을 더블클릭하면 바로 편집창이 열리도록 추가 | Playwright(자기 치유·삭제 차단·더블클릭 모두 실동작 확인) |
| 아이템 편집창 라우터 TAKE 버튼 (07-15) | Videohub 출력/입력 셀렉트가 값만 저장(`editDraft.router`)하고 실제 전환은 방송 중 온에어 시점(`applyItemToVMix`)에만 발사돼, 편집 중 선택해도 아무 반응이 없던 문제. TAKE 버튼 추가로 저장/방송과 무관하게 즉시 실제 크로스포인트 전환 발사(성공 토스트 포함, 기존 방송 중 자동 발사 경로는 그대로 무음 유지). 자막 섹션도 기타 섹션 바로 앞으로 재배치 | Playwright(버튼 연결·빈값 검증 확인; 실장비 연결 중이라 실제 크로스포인트 전환 자체는 미실시) |
| 중계 풀샷(기자 원샷) RS1~6 스위처 소스 추가 (07-15) | 라우터 크로스포인트 전환 자체는 연동돼 있었지만, 그 신호를 받는 vMix 인풋이 스위처 소스로 등록된 적이 없어 아이템 편집창 어디서도 "기자 원샷"을 고를 방법이 없었음. `switcherSources`에 rs1~6 추가(기존에 저장된 방송국 설정에도 1회성 마이그레이션으로 반영), vmixInput은 빈 값 — 실제 vMix 캡처 인풋 구성 후 설정 탭에서 이름 채우면 됨 | Playwright(마이그레이션 1회성·삭제 유지·중계풀샷/기타소스/시퀀스단계 드롭다운 전부 노출 확인) |
| 첫 화면 바로가기 추가 (07-15) | `index.html`에 MBCNewsTimeKeeper(외부 타이머 도구)·보도기술팀 공지사항 바로가기 버튼 추가 — 둘 다 별도로 호스팅되는 외부 도구라 새 탭으로 열림 | — |
| MARS 재전송 시 중계 시퀀스 phases-질문 수 어긋남 경고 (07-15) | `_preserveMasterOnlyItemFields()`가 MARS 재전송 때 `liveSeq.questions`만 최신값으로 갱신하고 저장된 `liveSeq.phases`는 그대로 두는데, `computeLivePhases()`는 phases가 있으면 questions를 아예 안 보므로 기사에서 질문 수가 바뀌어도 방송엔 옛 단계 수 그대로 나갈 수 있는 위험을 발견 — phases가 있는 상태에서 질문 수가 바뀌면 경고 로그를 남기도록 수정(phases 자동 재생성·삭제는 하지 않음, 기술감독이 "질문 수로 단계 자동 생성"으로 직접 판단) | 코드 추적 검증(브라우저 실행 검증은 미실시) |
| 큐시트 "진행" 표시 + 편집창 미리보기 (07-13) | 큐시트 표의 각 아이템 아래 실제 컷/디졸브 순서를 한 줄씩 표시(질문 1개=1줄, 현재+다음 3개만 기본 펼침·클릭 토글). 아이템 편집창 맨 아래에도 저장 전 실시간 미리보기 추가 — 라우터(RS#) 크로스포인트까지 포함해 카메라/DVE/서버 전체 컷 순서를 정교하게 확인 가능 | Playwright |
| 문서화 | 사용자 매뉴얼(user-guide/), 문서 통합 인덱스(doc/), 문제해결 가이드, 공개 문서 사이트(peter-cho-70/mars-master-docs, publish-docs.sh로 발행) | — |
| 라디오 등록 영상 2단계 준비/온에어 (07-16) | 라디오 모드 "등록된 영상" 버튼을 1클릭=재생 즉시 실행 방식에서 2단계로 분리 — 1차 클릭은 SelectIndex+Restart만 걸어 첫 프레임에 정지시킨 채 PVW 버스에만 올리고(재생 시작 안 함), 버튼 배경이 흰색으로 바뀌어 "준비됨"을 표시. 같은 버튼을 한 번 더 누르면 그제서야 Play+Cut을 곧바로 이어 걸어 재생과 동시에 온에어. 이 항목이 물린 서버가 지금 실제로 온에어 중이면(같은 서버 연속 재생) 준비 자체를 막고 경고 토스트만 띄움 — 온에어 화면을 몰래 건드리는 사고 방지. 뉴스 큐시트의 기존 단일 클릭 재생 흐름(`_vmixPlayFromList`)은 그대로 유지(`_vmixArmFromList`에 `autoPlay` 옵션 추가로 내부 분리) | Playwright(vMix 목킹 — SelectIndex/Restart/PreviewInput→Play/Cut 순서, 같은 서버 온에어 중 차단/다른 서버 정상 준비까지 실제 함수 호출로 검증; 실장비 미검증) |
| PGM 이탈 클립 자동 되감기 정지 (07-16) | 클립이 컷으로 PGM에서 내려가 프리뷰로 넘어갈 때 기존엔 `Pause`만 걸어 멈춘 그 중간 지점에 그대로 남아있어, 나중에 다시 그 클립으로 컷하면 멈췄던 자리부터 이어 재생되던 문제(`_vmixPollTick()`) — `Pause`→`Restart` 순서로 걸어 무조건 처음(0초)으로 되감아 정지된 채로 남도록 수정. 설정 탭의 "처음부터/이어서 재생" 체크박스는 클립이 끝까지 재생돼 자연 종료된 경우에만 적용되도록 안내 문구 정리(그 경로는 미변경) | Playwright(vMix 목킹 — PGM 전환 시 이전 클립에 Pause 다음 Restart가 실제로 호출되는 것 확인; 실장비 미검증) |
| 라디오 핫키 버튼 색상/크기 개선 (07-16) | 등록 영상·소스컷·자막 프리셋 버튼의 배경색이 알파 .18로 너무 옅어 서버별 색상 구분이 잘 안 보이던 문제 → 배경 알파 .55 + 2px 실색 테두리 + 흰 글자(그림자)로 대비 강화. 라디오 라이브 컷 패널(`#radio-cut-sec`) 버튼 패딩·폰트·크기(hk-size-1~3 폭)를 한 단계씩 키움 | Playwright(색상/테두리/크기 computed style 확인) |
| CG 자막 패널 입력 중 되돌림 버그 수정 (07-16) | 티커 등 CG 자막 입력창에 새 문구를 타이핑해놓고 "전송"을 누르기 전에 다른 자막 타입 전송/해제, 자막담당(cg-operator.html) 요청 폴링(1.5초), 큐시트 진행에 따른 자동 자막 등 무엇이든 먼저 발생하면 `renderCgOverlayPanel()`이 패널 전체를 다시 그리며 아직 안 보낸 값이 조용히 "마지막으로 보냈던 옛 문구"로 되돌아가던 버그 — 사용자 보고 증상: "티커자막이 새로 입력한 내용이 아닌 다른 내용이 나가고 있다". `renderLiveRouterOutputSelect()`에 이미 있던 것과 같은 패턴(포커스 보유 중이면 재렌더링 건너뜀)을 적용 | Playwright(실제 클릭·타이핑 → 다른 자막 전송으로 인한 재렌더링 → 입력값 보존 → 포커스 해제 후 정상 동기화까지 재현·검증) |
| 단신·리포트 앵커 단계 서버 소재 프리뷰 대기 (07-17) | 앵커 캠이 PGM에 나가는 동안 다음에 나갈 서버 클립을 vMix 프리뷰(PVW) 버스에 실제로 대기시키지 않던 버그 — 예전엔 `startAnchorPhaseVMix()`가 리스트 인덱스만 조용히 맞춰두고(SelectIndex만) `Function=PreviewInput`은 호출하지 않아, Master 화면의 "대기" 라벨만 바뀌고 실제 vMix 프리뷰 모니터에는 안 보였음(사용자 보고: "프리뷰에 서버소재가 준비가 되어있지 않아. 그래서 늦게 PGM으로 넘어와"). `updatePvwLookaheadVMix()`와 같은 방식(select+restart+PreviewInput)으로 통일 | Playwright(vMix 목킹 — 앵커 컷 직후 PreviewInput 호출과 PVW 전환까지 실제 함수 호출로 확인; 실장비 미검증) |
| Mix/Fade 전환 중 다음 아이템 프리뷰 개입 버그 수정 (07-17) | 단신 아이템이 앵커→영상 Mix(디졸브) 전환 중일 때, `cutToVideoVMix()`가 전환이 끝나길 기다리지 않고 곧바로 다음 아이템 프리뷰 예약(`updatePvwLookaheadVMix`)의 `Function=PreviewInput`을 호출해 vMix가 진행 중이던 디졸브 대상을 잃고 화면이 앵커 카메라로 되돌아가던 버그(사용자 보고) — REPORT처럼 기본 전환이 Cut(순간적)인 아이템은 이 시간차가 없어 증상이 없었음. 전환 지속시간만큼 기다린 뒤 다음 아이템 프리뷰를 건드리도록 수정(`startAnchorPhaseVMix()`가 이미 쓰던 것과 같은 패턴) | Playwright(vMix 목킹 — Fade(Duration=500) 호출 후 503ms 뒤에야 다음 아이템 PreviewInput이 발사되는 것을 타임스탬프로 확인) |
| 진행창 잔여/총 시간 실측 표시 (07-17) | 진행창의 "잔여 X / 총 Y"가 실제 재생 영상과 무관하게 큐시트에 적어둔 예정 시간(item.dur)만 로컬에서 카운트다운하던 것을, 영상이 실제 재생 중이면 vMix의 실측 duration/position 기준으로 보여주도록 변경. "총"은 앵커가 실제로 멘트를 읽은 시간(`ancPhaseElapsedSec`, 영상 전환 시점에 기록) + 영상 실제 길이의 합으로 계산(사용자 설명: "실제 아이템 길이는 앵커가 이야기한 내용과 영상소재 재생된 길이") | 코드 추적 검증(브라우저 실행 검증은 미실시) |
| 방송 시작 전 아이템 스탠바이 + "▶ 시작" 버튼 녹색 표시 (07-17) | 큐시트 첫 아이템 등 영상 소재가 있는 아이템을 방송 시작 전에 선택하면(행 클릭) 실제 온에어 때와 같은 서버 예측식으로 그 클립을 미리 vMix 프리뷰에 대기(`selectItem()`에 추가)시키고, "▶ 시작" 버튼이 초록색 "🟢 스탠바이"로 바뀌어 준비 완료를 알려줌 — 실제 시작을 누르면 원래의 빨간 "▶ 시작"으로 복귀 | Playwright(vMix 목킹 — 선택 시 PreviewInput 발사·버튼 텍스트/클래스/배경색 변화, 이어지는 실제 시작과의 충돌 없음까지 확인) |
| 영상 재생 타이밍 기본값 0으로 전환 (07-17) | 프리롤(초)·시작 후 프레임 건너뛰기의 기본값을 각각 1초/10프레임에서 **0**으로 변경 — 방송국·PC마다 필요한 여유가 달라 하드코딩된 기본값 대신 운영자가 설정 탭에서 직접 채워 넣는 값으로 전환(끝나기 전 정지 15프레임은 실측 검증값이라 그대로 유지). 설정 탭 안내 문구도 새 기본값에 맞게 갱신 | 코드 검토 |
| MARS 중계 시퀀스 소스 목록 RS1~6 누락분 추가 (07-17) | Master의 스위처 소스에는 07-15에 RS1~6(중계 풀샷)이 추가됐지만, MARS 아이템 편집창의 "중계 시퀀스" 단계별 소스 드롭다운(`LIVESEQ_SRC_OPTIONS`)에는 반영이 안 돼 있어 "기자풀샷"을 시퀀스 소스로 지정할 방법이 없었음 — Master의 id 스페이스와 맞춰 6개 옵션 추가 | 코드 검토 |
| MARS 자막 자동 타이밍 기본값을 수동으로 전환 (07-17) | 단신 자막의 "영상 길이 기준 자동 타이밍"(`videoBasedTiming`)과 "CG In 딜레이"(`cgInDelay`)가 예전엔 기본값이 자동(각각 `!== false`, 암묵적 2초)이라, 아무것도 지정하지 않은 자막까지 방송 중 저절로 자동 전송되던 문제("자동이 디폴트" 문제) — 기본을 수동(값 미지정 시 Master CG 큐에서 ON 버튼으로 수동 전송)으로 반전시키고, 명시적으로 켠 경우만 자동 계산하도록 수정 | 코드 검토 |
| 세션 마감 자체 코드 리뷰 (07-17) | 오늘 작업한 두 파일의 변경분을 8개 관점(라인별 스캔·제거된 동작 감사·호출부 추적·재사용·단순화·효율·설계 깊이·CLAUDE.md 준수)으로 자체 리뷰, 실제 버그 9건 발견 즉시 수정 — ①`startCurrent()`가 순차형(단신/리포트) 아이템 시작 때 `lastPlayedSrvN`(직전 아이템이 남긴 값)을 "지금 온에어 중인 서버"로 잘못 넘겨 다음 아이템 프리뷰 대기를 잘못 건너뛸 수 있던 버그 ②Mix/Fade 전환 중 다음 아이템 프리뷰 개입 버그가 `cutToVideoVMix()` 한 곳만 고쳐져 있어 `startCurrent()`를 타는 모든 진입 전환(TITLE/CM/WEATHER/CLOSING 등)에 그대로 남아있던 문제 — 원인 자리(`startCurrent()`)에서 일괄 수정 ③`cutToVideoVMix()`의 전환 대기 중 `nextCue()`가 끼어들면 엉뚱한 다음 아이템을 프리뷰에 밀어넣을 수 있던 경합 ④CG 자막 자동 타이밍을 수동 기본값으로 바꾼 수정이 vMix 자막(`applyCGPresetVMix`)에만 적용되고 브라우저 오버레이(`applyCGPresetBrowser`)엔 빠져있던 문제 ⑤클립이 여러 개인 아이템에서 진행창 "총" 시간이 클립이 바뀔 때마다 줄었다 늘었다 하던 버그(클립 1개 아이템만 실측 표시하도록 범위 조정) ⑥MARS "지우기" 버튼 주석이 "자동으로 되돌아간다"고 남아있었지만 기본값 반전 이후 실제로는 수동으로 돌아가 코드·설명 불일치 ⑦~⑨"다음 ▶" 버튼과 제작(LIVE) 큐시트에도 스탠바이 관련 개선 일관 적용, 진행창 타이머의 로컬 1초 카운트다운과 vMix 폴링 실측 동기화가 서로 다투며 화면이 순간 어긋나 보이던 문제 수정 | 8-에이전트 병렬 리뷰 + Playwright(vMix 목킹) 재검증 |
| 서버 클립 리스트 자동 전개 고속 감시 추가 (07-17) | 실사용 중 "타이틀이 다음 아이템 소재까지 재생하다가 멈춘다" 보고 — 클립 재생 직후 vMix가 duration을 한동안 0으로 보고하면(타이틀류 등 짧은 소재에서 재현) 정밀 정지 타이머(`scheduleServerClipAutoStop`)가 아예 안 걸리고 기존 1.5초 폴링 백업에만 의존해, 자연 종료~다음 폴링 사이 최대 1.5초 동안 vMix가 이미 다음 리스트 항목(다음 아이템 소재)을 재생할 수 있었음 — duration 재시도 횟수를 4회(0.8초)→10회(2초)로 늘리고, 기존 1.5초 폴링과 별개로 0.3초 주기 전용 고속 감시(`_srvFastSafetyTick`)를 추가해 노출 창을 5배 줄임 | Playwright(vMix 목킹 — duration 항상 0 + 재생 2초 뒤 vMix 자체 리스트 전개 재현, 새 고속 감시가 즉시 Pause 거는 것을 타임스탬프로 확인) |
| 아이템 편집창 실화면 캡처 가이드 신설 (07-17) | `user-guide/item-editor-guide.html` — Master·MARS 양쪽 아이템 편집창 전체 필드를 실제로 채운 화면 캡처(번호 표시 포함), 중계(LIVE) 시퀀스 채우기, MARS 자막(CG) 설정 상세, 영상 보며 자막 IN/OUT점 찍기, Master·MARS 자막 설정 차이 비교까지 정리. getting-started-manual.html의 관련 절에서 링크 연결 | Playwright(실제 아이템 추가→필드 채움→스크린샷, 실제 vMix 매체 파일로 IN/OUT 재생 확인) |
| 방송 시작 전 서버 소재 PGM 사전 배치 — 컷 전환 없는 시작 (07-17) | 방송 시작 전 아이템을 선택하면(서버 클립형) 기존엔 PVW에만 대기시켰는데, 사용자 요청("컷 전환 자체가 안 보이면 좋겠다", "방송도 실제로는 처음에는 PGM에 방송 소재를 놓고 시작한다")에 따라 PVW 대신 곧바로 PGM에 컷해 정지된 채로 얹어두도록 변경(`armSelectedItemOnPGM`). "▶ 시작"을 누르면 이미 PGM에 있는 소재이므로 Select/Restart/Cut을 다시 걸지 않고 Play만 호출해 컷처럼 보이는 전환 자체가 없음(`applyItemToVMix`가 `_pgmStandbyItemId`/`_pgmStandbySrvName` 일치 여부로 판단). 카메라 진입형은 기존 PVW 대기 방식 유지(범위 밖) | Playwright(vMix 목킹 — 선택 시 SelectIndex→Restart→Cut(PGM)까지만 발생, "▶ 시작" 시 Function=Play 단독 호출만 발생하는 것을 실제 함수 호출로 확인; 미선택 상태에서 GO로 바로 시작하는 기존 경로도 그대로 SelectIndex→Restart→Play→전환효과 순으로 동작하는 것 함께 확인) |
| PGM 사전 배치 직후 자동 재생 방지 + 대기 중 안전장치 등록 (07-17) | 실사용 중 "아이템만 선택해도 바로 재생되고 있다"/"타이틀이 여전히 다음 아이템까지 넘어간다" 보고 — 일부 vMix 환경에서 `Function=Cut`으로 PGM에 올라오는 순간 리스트 클립이 자체적으로 재생을 시작하는 것으로 확인됨(Restart로 0초를 맞춰둬도 Cut 자체가 트리거). `armSelectedItemOnPGM()`이 Cut 직후 `Function=Pause`를 명시적으로 걸어 vMix의 Cut 동작 방식에 기대지 않고 정지 상태를 강제하고, 대기 단계에서부터 `srvActiveClipIdx`에 등록해 기존 안전장치(0.3초 고속 감시·1.5초 폴링)가 방송 시작 전부터 이 서버를 지켜보도록 함 — 혹시 재생이 시작돼도 자연 종료/리스트 이탈 시 즉시 정지되어 다음 리스트 항목으로 넘어가지 않음 | Playwright(vMix 목킹 — Cut이 VideoList를 자체적으로 재생시키는 상황을 재현해도 뒤이은 Pause 호출로 최종 상태가 Paused임을 확인, srvActiveClipIdx 등록 및 이어지는 "▶ 시작" 시 Play 단독 호출까지 재검증) |
| "완료" 버튼 — 재생 중지 후 스탠바이 복귀 (07-17) | 사용자 요청: "완료를 누르면, 재생되고 있는 것을 멈추고 그냥 스탠바이가 되도록". `markDone()`이 이제 지금 추적 중인 서버 소재를 전부 Pause→Restart(처음 0초로 되감기)하고 `srvActiveClipIdx`에서 정리, 예약된 영상 조기전환·CG 자동 타이머도 함께 취소 | Playwright(vMix 목킹 — 재생 중 완료 클릭 시 Pause→Restart 순서, 위치 0 복귀, srvActiveClipIdx 정리까지 실제 함수 호출로 확인) |
| 디졸브(Mix) 전환 중 재생이 멈추는 경합 수정 (07-17) | 사용자 보고("디졸브로 넘어갈 때 멈추는 현상이 지금도 발생") 원인 규명 — PGM 사전 배치를 시작하면서 `srvActiveClipIdx`에 조기 등록하게 된 이번 세션의 변경이, 아이템을 스탠바이만 해두고 시작하지 않은 채 그 서버가 나중에 다른 아이템(회전)에 재사용되면 낡은 인덱스가 남아있게 만들었고, `_vmixArmFromList()`가 SelectIndex→Restart→Play 단계를 밟는 도중(아직 `_markSrvActive()`로 새 인덱스가 반영되기 전) 0.3초 고속 감시/1.5초 폴링이 그 낡은 값과 비교해 "인덱스가 벗어났다"고 오판, 지금 막 디졸브로 걸리고 있는 재생을 즉시 멈춰버릴 수 있었음 — `_vmixArmFromList()` 시작 시점에 유예시간(`srvActiveSetAt`)을 곧바로 갱신하고, 두 안전장치 모두 이 유예시간을 확인하도록 수정, `armSelectedItemOnPGM()`이 다른 아이템을 스탠바이로 바꿀 때 이전 서버의 낡은 등록도 정리하도록 추가 | Playwright(정확히 이 경합 상황을 인위적으로 재현 — 낡은 인덱스+오래된 유예시간을 심어두고 새 arm 시퀀스와 고속 감시를 동시에 실행해도 더 이상 Pause가 잘못 걸리지 않는 것을 실제 함수 호출로 확인) |
| 서버소스 "대기 단계" 방어 누락분 일괄 점검·수정 (07-17) | 사용자 보고("라디오 등록된 영상, 스탠바이일 때 미리 재생이 되는 경우가 발생했고, 다음 리스트 항목까지 넘어가는 상황도 발생") — `armSelectedItemOnPGM()`엔 이미 적용돼 있던 "대기 단계 방어"(SelectIndex/PreviewInput만으로도 실제 재생이 시작될 수 있어 곧바로 방어적 Pause + 대기 시점부터 `srvActiveClipIdx` 등록)가, 서버소스를 미리 세워두는 다른 세 곳 — 라디오 "등록된 영상" 1차 클릭(`playRadioMediaItem`), 다음 아이템 프리뷰 대기(`updatePvwLookaheadVMix`), 단신/리포트 앵커 단계 영상 사전 로드(`startAnchorPhaseVMix`) — 에는 빠져 있었음. 대기만 해두고 그 서버가 실제로 재생을 시작해버려도 아무도 감시하지 않아 자연 종료 시 다음 리스트 항목으로 그대로 넘어갈 수 있었던 것이 근본 원인. 세 곳 모두 동일하게 방어적 Pause + `_markSrvActive` 등록을 추가하고, 라디오 경로엔 `armSelectedItemOnPGM()`과 같은 "다른 서버로 재준비 시 이전 서버의 낡은 등록 정리"도 함께 추가 — 사용자 지시("서버소스를 재생하는 경우에는 무조건 이 문제는 신경을 써서 수정") 에 따라 서버소스를 세워두는 코드 경로 전체를 훑어 동일 패턴을 강제 | Playwright(vMix 목킹 — 라디오 1차 클릭 직후 Pause 호출 및 `srvActiveClipIdx` 즉시 등록 확인, 유예시간 경과 후 리스트가 몰래 다음 항목으로 넘어간 상황을 재현해 고속 감시가 Pause로 잡아내는 것 확인, 온에어 없이 다른 서버의 항목을 준비하면 이전 서버의 등록이 정리되는 것까지 실제 함수 호출로 검증) |
| 대기 단계 Pause 이후 위치가 0초로 재확정되지 않아 "처음부터 재생 안 됨" (07-17) | 사용자 보고("실제 재생하면 처음부터 재생되지 않고 일부만 재생되다 커트됨") 원인 규명 — 바로 위 항목에서 4곳(`armSelectedItemOnPGM`/`playRadioMediaItem` 1차 클릭/`updatePvwLookaheadVMix`/`startAnchorPhaseVMix`)에 추가한 방어적 Pause는 Cut·PreviewInput이 먼저 재생을 몇 프레임 진행시킨 "이후" 시점에 걸리므로, 정지되는 position이 정확히 0초가 아닐 수 있음 — Restart는 재생을 다시 시작하지 않고 위치만 0으로 되돌리는 함수인데, 그동안 Pause 다음에 이 Restart를 다시 걸어주지 않아 "대기 화면은 정지된 중간 지점"인 채로 남아있었고, 이후 실제 온에어 시점(`applyItemToVMix`의 `_preArmedOnPGM` 분기, `playRadioMediaItem`의 2차 클릭)이 재선택 없이 곧장 `Play`만 호출해 그 중간 지점부터 이어 재생됐던 것이 근본 원인. 4곳 모두 대기 단계의 Pause 직후 Restart를 추가해 대기 위치를 항상 0초로 재확정 | Playwright(vMix 목킹, 실제 함수 호출) — 대기 단계 호출 순서가 `SelectIndex→Restart→Cut/PreviewInput→Pause→Restart`임을 실제 vmixCall 로그로 확인. 대기 등록 후 vMix 리스트가 몰래 다음 항목으로 넘어간 상황(0.3초 고속 감시 `_srvFastSafetyTick`)도 재현해 Pause로 즉시 잡아내고 등록이 정리되는 것까지 검증 |
| 위 수정 직후 "컷은 되는데 재생이 시작 안 됨" 회귀 → 온에어 직전 이중 Restart 제거 (07-17) | 위 수정 때 이중 안전장치로 `applyItemToVMix`의 `_preArmedOnPGM` 분기와 `playRadioMediaItem` 2차 클릭(온에어) 양쪽에 "Play 바로 직전 Restart"도 함께 걸어뒀는데, 사용자가 실제 vMix로 테스트하자마자 "등록된 영상을 테스트하는데 컷트되고도 재생이 되지 않는다"는 새 증상이 나타남 — Restart와 Play를 거의 동시에 연달아 보내면 vMix 엔진이 재생 명령을 놓치는 것으로 추정(실제 vMix 엔진 내부 타이밍 문제라 vMix 목킹으로는 재현되지 않았고, 실장비 테스트로만 드러남). 위치를 0초로 되돌리는 책임은 이미 대기 단계(Pause 직후)에서 Play와 충분한 시간 간격을 두고 처리되므로, 온에어 직전의 이 이중 Restart는애초에 불필요한 안전장치였음 — 두 곳 모두 제거하고 기존 `Play`(→`Cut`) 흐름으로 되돌림 | Playwright(vMix 목킹) — 되돌린 뒤 온에어 호출 순서가 다시 `Play→Cut`(라디오), `Play`만(뉴스 큐시트 PGM 사전배치 소비)로 나오는 것을 실제 함수 호출로 재확인. 대기 단계의 Restart-after-Pause(0초 재확정)는 그대로 유지 — 실장비 재검증 필요 |

| MARS 자막 종류 이름을 Master 기준으로 통일 (07-18) | 사용자 보고("마스 큐시트 아이템의 자막 종류가 마스터와 다르다") → "이름(라벨)이 다르다"로 확인. 키 집합(8종: lower/name/break/ticker/bottom/time/loc/corner)은 이미 두 프로그램이 동일했지만, 화면에 보이는 이름은 각자 독립된 localStorage(Master: `nbsVMixCGConfig`, MARS: `marsSubCgLabels`)에 따로 저장돼 있어 한쪽에서 이름을 바꿔도 다른 쪽엔 반영되지 않았음(MARS엔 자체 이름 변경 UI까지 있었음). Master를 진실의 원천으로 삼아 server.py에 `GET/POST /api/cg-labels`(Videohub·CG요청과 같은 "얇은 릴레이" 패턴, 메모리 저장) 추가 — Master는 설정 저장 시·페이지 로드 시 자기 이름을 밀어주고(`pushCgLabelsToServer`), MARS는 페이지 로드 시 받아오며(`fetchMasterCgLabels`) 자체 이름 변경 UI는 제거하고 "🔄 이름 새로고침" 버튼으로 대체 | Playwright(실제 서버 재시작 후 실동작 확인) — Master에서 코너자막 라벨을 테스트 값으로 바꿔 저장 흐름과 동일하게 `pushCgLabelsToServer()` 호출 → `/api/cg-labels`에 반영 확인 → MARS를 새로 열어 페이지 로드 시 자동으로 그 값을 받아오는 것과 수동 새로고침 버튼 모두 확인 후 원래 값으로 복구 |

| 실제 vMix에 제목자막이 안 나가던 원인 — GT 인풋/필드 설정 미매칭 (07-18) | 사용자 보고("MARS→Master 핸드오프는 정상인데 실제 vMix엔 자막이 안 나온다") — 실제 vMix(192.0.2.2)에 직접 연결해 확인한 결과, Master "자막 설정"의 제목자막(lower) 대상이 코드 기본값 그대로(`GT타이틀1` 인풋, `자막텍스트` 필드)였는데 실제 vMix엔 `GT타이틀1`이라는 인풋 자체가 없어(24개 인풋 전수 조회로 확인) SetText/OverlayIn이 존재하지 않는 인풋을 향해 나가 아무 효과가 없었던 것이 원인. 실제 vMix의 진짜 GT 인풋은 `bokdea.gtzip`(필드 `TextBlock1`)이 제목자막 용으로 보여(과거 테스트 문구 "빠르게 진행하고 있어요." 잔존) 그쪽으로 설정을 고쳐 저장 — 다른 자막 종류(이름/티커/하단 등, `namebokdea.gtzip`/`Ticker.gtzip`/`Text.gtzip`/`mbc-low·high·hand.gtzip` 후보 존재)는 사용자 확인 후 추후 마저 고칠 필요 있음(진행 중 항목에 남김) | 실제 vMix(오버레이 채널은 계속 꺼둔 채 SetText만) — 고친 인풋/필드로 텍스트를 보내 실제로 vMix `bokdea.gtzip`의 `TextBlock1.Text`가 갱신되는 것을 vMix 상태 XML로 직접 확인 후 원래 값으로 복구 |

| 자막 설정 카드 — 다른 카드 조작 시 라벨/GT인풋명/오버레이슬롯 리셋 (07-18) | 사용자 보고("설정에서 내가 다시 하고 있는데, 리셋이 되지 않도록 해줘") — "vMix GT 타이틀(자막)" 8개 카드 중 라벨/GT인풋명/오버레이슬롯 입력은 어떤 JS 상태에도 동기화되지 않고 DOM에만 있다가 저장 시점에만 읽혔는데, 다른 카드에서 "🔍 감지"·"+ 필드 추가"·필드 삭제·감지 결과 클릭(모두 `renderVmixCgConfigCards()`로 카드 8개 전체를 다시 그림) 중 하나라도 누르면 아직 저장 안 한 다른 카드의 값까지 마지막 저장값으로 덮어써져 리셋되던 것이 원인 — 필드 목록(`vmixCgDraftFields`)과 같은 패턴으로 라벨/GT인풋명/슬롯도 작업 중 상태(`vmixCgDraftMeta`)에 저장해 재렌더에도 유지되도록 수정, "GT 인풋 후보 클릭"/"대소문자 고치기" 버튼 2곳도 DOM 값만 바꾸고 draft는 안 바꾸던 같은 버그라 같이 수정 | Playwright(실제 함수 호출) — 한 카드에 값을 입력해두고 다른 카드의 "필드 추가" 버튼으로 전체 재렌더를 유발해도 입력값이 그대로 유지되는 것을 확인 |

| 자막 설정 — 기본값 저장/불러오기 (07-18) | 사용자 요청: "지금 설정된 자막을 기본값으로 해서 불러오기 기능을 만들어줘. 기본값 설정 버튼도" — GT 인풋/필드를 실장비에 맞춰 어렵게 매핑해둔 뒤, 나중에 실수로 값이 바뀌거나 다른 PC로 옮겼을 때 원클릭 복구할 수 있게 함. "⭐ 지금 설정을 기본값으로"(현재 화면을 먼저 실제 저장한 뒤 별도 `nbsVMixCGConfigDefault` 슬롯에 스냅샷 복사, 저장 시각 표시) / "↺ 기본값 불러오기"(그 스냅샷을 지금 설정에 덮어씀, 확인창 거침) 버튼 추가 | Playwright(실제 함수 호출) — 지금까지 설정한 5종 매핑을 기본값으로 저장 → 일부러 값을 깨뜨림 → 불러오기로 원래 값이 정확히 복구되는 것까지 확인 |

| CM 등 디졸브(Mix) 전환이 끝나자마자 멈추는 문제 재발 — 유예시간이 전환 소요시간을 못 덮음 (07-18) | 사용자 보고("CM에서 디졸브가 되고나서 멈추고 있어") — 07-17에 한 번 고쳤던(`_vmixArmFromList()` 시작 시점 유예 500ms) 것과 같은 계열의 경합이지만 다른 지점: 그 수정은 SelectIndex/Restart/Play 각 API 호출의 왕복 지연만 가정했을 뿐, 그 다음 실제로 걸리는 디졸브 전환 자체의 소요시간(Duration, 기본 Mix 프리셋 500ms)은 유예에 반영하지 못했다 — 화면에서 디졸브가 실제로 끝나기도 전에 유예가 먼저 만료돼 안전장치(0.3초 고속 감시·1.5초 폴링)가 "인덱스가 벗어났다"고 오판, 지금 막 디졸브로 전환 중인 재생을 멈춰버릴 수 있었다(기본 Mix Duration이 유예와 같은 500ms라 여유가 전혀 없어 네트워크 지연에 따라 간헐적으로 재현). `_vmixCut()`이 Duration 있는 전환을 걸 때마다 `srvActiveSetAt`을 "지금+Duration"으로 미래까지 늘리도록 수정, 그 직후 호출되는 `_markSrvActive()`가 무조건 `Date.now()`로 덮어써 이 연장을 지워버리던 것도 `Math.max()`로 바꿔 보존되도록 함께 수정 | Playwright(vMix 목킹, 실제 네트워크 지연을 흉내낸 지연 응답) — 1.5초급 긴 전환을 걸어두고 전환 소요시간 안쪽(옛 500ms 유예라면 이미 만료됐을 시점)에 고속 감시를 돌려도 더 이상 잘못 멈추지 않는 것을 실제 함수 호출로 확인 |

| 스탠바이/디졸브 재생 불일치 재발 — Restart가 재생을 재개할 수 있음 + 정밀 정지 루프가 전환 유예를 안 봄 (07-18) | 사용자 보고("디졸브 이후 소재가 멈추는 현상이 또 발생", "아이템만 눌렀는데 PGM에서 바로 실행됨 — 전엔 안 그랬음", "2번째 단신은 디졸브 후 멈추고, 시작을 눌러도 조금 진행하다 다시 멈춤 — 일관성이 없다") — 두 가지가 겹친 원인으로 규명. ① 지금까지 모든 대기(스탠바이) 시퀀스는 "Pause 걸고 Restart로 0초 확정하면 무조건 멈춘 상태가 된다"고 가정한 채 명령만 보내고 끝났는데, 이 가정 자체가 이미 두 번 깨진 전례가 있다(Cut의 자체 재생 트리거, 라디오 등록영상의 "스탠바이인데 미리 재생") — Restart도 같은 종류의 가정이라 이 하드웨어에서 항상 보장된다고 볼 수 없고, 기존 안전장치(0.3초/1.5초)는 "선택 인덱스가 맞는지"만 보고 "지금 재생 중인지"는 전혀 보지 않아 조용히 재생을 시작해도 못 잡아낸다(선택하자마자 PGM에서 바로 실행되는 증상과 정확히 일치). ② `scheduleServerClipAutoStop()`의 정밀 정지 루프(위치가 거꾸로 줄면 "이미 다음 리스트 항목으로 넘어갔다"고 보고 즉시 정지)는, 앞서(07-18) 두 안전장치에만 추가했던 디졸브 유예시간(`srvActiveSetAt`)을 전혀 확인하지 않고 있었다 — 전환 직후 position 판독이 흔들리면 디졸브가 끝나자마자 스스로 정지시킬 수 있는, 이전 수정이 놓친 세 번째 경로였다. 명령을 보낸 뒤 실제 state를 다시 읽어 확인하고 아직 재생 중이면 재시도하는 `_vmixEnsurePaused()`를 새로 만들어 대기 시퀀스 6곳(`armSelectedItemOnPGM`/`startAnchorPhaseVMix` 사전로드/`updatePvwLookaheadVMix`/`playRadioMediaItem` 1차클릭/PGM 이탈 자동 되감기/`markDone`)에 모두 적용, 정밀 정지 루프에도 같은 유예시간 확인을 추가, 디졸브 유예 자체에도 300ms 안전 여백을 더함 | 코드 추적 검증 + 문법·로드 확인(Playwright) — vMix 미연결 상태에서 작업해 실제 하드웨어 타이밍 재현은 못 함, **다음 실장비 세션에서 반드시 재검증 필요** |

| CG 스튜디오(`cg-studio.html`) 신설 — 자막담당 화면 리디자인 (07-18) | 사용자 요청: "cg-operator.html(방법2, `/api/cg-request` 릴레이)로 자막을 처리하는 별도 화면을 탁월한 UI/UX로 만들어달라" — 기존 cg-operator.html은 그대로 두고 완전히 새 파일로 추가. 큐시트 검색·8종 자막 타입 카드(모두 항상 보임, 타입당 필드 수는 obs-cg-overlay.html이 실제로 그리는 구조를 기본값으로 — 제목자막/이름자막만 2필드)·Alt+1~8 단축 포커스·아이템별 사전 등록 자막 "즉시전송/불러오기" 분리·이 브라우저 전용 즐겨찾기 문구·2단계 확인 전체해제·`/api/cg-state` 실시간 폴링으로 카드 LED·값 프리필(포커스/타이핑 중엔 안 덮어씀)·`/api/cg-labels` 자동 동기화(이름 바꿔도 재접속 불필요)까지 구현. 제작 과정에서 실버그 2건 발견·수정: 프리셋 "불러오기"가 값을 넣자마자 다음 폴링에 라이브 상태로 도로 지워지던 문제(포커스만 걸고 "손댔음" 표시를 안 남겨서), 버튼 클릭 시 브라우저 기본 포커스가 나중에 실행되며 입력창 포커스를 도로 뺏어가던 문제 → 둘 다 실제로 재현한 뒤 수정 확인. 좁은 화면(태블릿/모바일)에서 상단바 텍스트가 줄바꿈되며 58px 높이를 넘어 본문 위로 겹쳐 보이던 반응형 버그도 실측 발견 후 min-width:0/nowrap+말줄임+단계별 텍스트 숨김으로 수정 | Playwright(실제 서버 기동, 큐시트 로드·아이템 선택·프리셋 불러오기/즉시전송·카드 전송·즐겨찾기 저장/적용·Alt+숫자 단축키·2단계 전체해제·1500/820/560px 반응형까지 전부 실동작 확인; Master 미실행 환경이라 실제 vMix 반영 자체는 미검증) |

| CG 스튜디오 "커스텀 자막" 탭 신설 — vMix GT/Master를 아예 안 거치는 순수 HTML 템플릿 자막 (07-18) | 사용자 요청: "obs-cg-overlay 샘플처럼 vMix 자막을 이용하지 않고, HTML에서 자막 템플릿을 만들고·표시하고·인/아웃을 직접 할 수 있는 기능을 CG 운영 화면에 별도로 만들어달라" — cg-studio.html에 3번째 탭 추가. HTML/CSS를 직접 작성하는 템플릿 편집기(`{{필드명}}` 자리표시자를 쓰면 오른쪽 방송 제어판 입력창이 자동 생성) + 체크무늬 배경의 실시간 미리보기(iframe srcdoc, 실제 오버레이와 동일한 뼈대 사용) + 표시(IN)/내리기(OUT) 버튼. 아키텍처: 기존 8종 CG처럼 Master 릴레이(`/api/cg-request`)를 타지 않고 새 `/api/custom-templates`(템플릿 정의, `custom-cg-templates.json`에 영속화)·`/api/custom-cg-state`(지금 방송 화면 상태, 메모리)를 cg-studio.html이 직접 호출 — Master가 꺼져 있어도 동작한다는 뜻. 새 렌더러 `cg-custom-overlay.html`(obs-cg-overlay.html과 같은 자리에 vMix Browser 입력/OBS 브라우저 소스로 등록)이 그 상태를 폴링해 템플릿의 HTML/CSS를 그대로 그리고 필드값만 이스케이프해 채워 넣는다. 기본 예시 템플릿 2종(하단 타이틀/코너 배지)을 미리 채워둠. 제작 중 실버그 2건 발견·수정: cg-custom-overlay.html이 상태와 템플릿 목록을 각각 폴링하는데 상태가 템플릿보다 먼저 도착하면 그 뒤로 영영 안 그려지던 경합(서명 비교로 재시도하도록 재설계), 새로 만든(아직 저장 안 한) 템플릿은 방송 제어판 자체가 숨겨져 있어 "표시(IN)"를 누를 방법이 없던 문제(제어판을 항상 보이게 하고 IN을 누르면 자동 저장 후 송출하도록 수정) | Playwright(실제 서버 기동, 새 템플릿 생성→필드 자동감지→미리보기→자동저장 후 IN→별도 탭에서 cg-custom-overlay.html이 실제로 렌더링하는 것까지 스크린샷으로 확인, OUT으로 opacity 0 복귀 확인, 삭제 시 라이브 상태도 같이 꺼지는 것까지 확인; 실제 vMix Browser 입력 등록 자체는 미검증) |

| 아이템 전환 시 이전 자막이 안 지워지고 다음 아이템에 잠깐 겹쳐 보임 (07-18) | 사용자 보고("뉴스자막이 각 아이템마다 실행해보니 그 전 아이템 내용이 지워지지 않고 먼저 나왔다가 해당 아이템 내용이 나온다") — 원인 규명: `clearCGAutoTimers()`(새 아이템 CG 계획 시작·앵커 단계 진입·중계 시퀀스 시작·완료·전체 리셋 등 아이템/단계가 바뀌는 지점마다 이미 호출되고 있던 함수)가 이름 그대로 "타이머만" 취소했지, 그 시점에 이미 화면에 나가 있는 자막 자체(OUT을 수동으로 남겨둔 타입, 또는 OUT 타이머가 아직 안 돈 채 다음 아이템으로 넘어간 경우)는 그대로 켜둔 채 방치하고 있었다 — 그래서 다음 아이템 자신의 자막이 뜨기 전까지 이전 아이템 자막이 그대로 남아 보이는 것이 근본 원인이었다. `clearCGAutoTimers()`에 `clearAllCGVMix()`/`clearAllCGBrowser()` 호출을 추가해, 아이템/단계가 바뀌는 모든 지점에서 지금 떠 있는 자막을 전부 내리고 다음 아이템이 항상 빈 보드에서 시작하도록 수정 | 코드 추적 검증 + Playwright(vmixConnected=false·브라우저 오버레이 꺼짐 상태에서 예외 없이 동작하는 것 확인) — 실제 vMix로 아이템 연속 전환 시 자막이 실제로 깨끗이 넘어가는지는 다음 실장비 세션에서 재검증 필요 |

| DVE(vMix Layers 화면분할) 레이어 원격 제어 신설 — 출연/중계 시퀀스 연동 (07-18) | 사용자 요청: "출연과 중계에 쓰는 DVE1,2,3 화면분할을 초기 설정만 해두면 리모트에서 변경할 수 있게 해달라" — 지금까지 DVE1/2/3은 스위처 소스로 "완성된 화면 하나"만 등록해 컷/디졸브 대상으로만 썼고, 그 안의 레이어(예: 서버 갈라치기 DVE의 "서버 자리")를 원격으로 바꾸는 기능은 전혀 없었다(서버는 SRV1~3로 방송 중 교번하는데 DVE 안의 레이어는 고정이라 실제 온에어 서버와 어긋날 수 있는 구조적 공백). 3단계로 구현: ① `switcherSources[i].layers`(레이어별 idx·이름·기본 소스) 데이터 추가 + 설정 탭 "스위처 소스 관리"의 각 행에 접이식 "레이어" 편집 패널(추가/삭제, vMix 실측 확인용 "지금 기본값 적용" 테스트 버튼) — 특정 id로 제한하지 않고 모든 소스에 범용 적용. ② vMix REST API `SetLayer` 함수를 감싼 `_vmixSetLayer()`/`applySourceLayers()` 신설 — 합성 인풋을 실제 PGM에 컷하기 "전"에 레이어부터 채워 넣는다(먼저 컷하면 레이어가 바뀌는 순간이 방송에 그대로 노출됨). ③ LIVE 시퀀스 엔진(`advanceLiveSeq`)이 갈라치기 단계로 넘어갈 때마다 자동으로 `applySourceLayers()`를 호출하도록 연동, 아이템 편집창의 시퀀스 단계별로 레이어 기본값을 개별 재정의(`phase.layers`)할 수 있는 드롭다운도 추가, "진행 미리보기"에도 `[L1=SRV2]`처럼 재정의값을 표시 | Playwright(vmixCall을 가로채 실제 발사되는 SetLayer 요청 문자열 확인 — 기본값·재정의값 양쪽 케이스 모두 정확한 Input/SelectedIndex/Value로 나가는 것 확인, 설정 탭 레이어 추가/삭제 UI 실동작 확인, 아이템 편집창에 실제로 레이어 재정의 드롭다운이 뜨고 진행 미리보기에 반영되는 것까지 실제 모달로 확인) — `SetLayer` 함수명/파라미터는 vMix 공식 문서 기준으로 작성했으나 이 프로젝트는 문서와 실장비 동작이 달랐던 전례가 여러 번 있어 **다음 실장비 세션에서 정확한 파라미터 확인 및 vMix Layers 순번(idx) 실측 필요**(vMix 설정 → Web Controller에서 확인 권장) |

| DVE 레이어 — 사전 준비(프리암)·아이템 종료 시 초기화·화면 확인 기능 추가 (07-18) | 사용자 요청 3가지: "DVE 설정이 변화 가능하니까 사전에 미리 준비가 되어야 한다", "자막처럼 해당 아이템이 끝나면 다음 아이템에 영향을 주지 않도록", "DVE 값으로 소재가 정상적으로 화면이 나오는지 확인하는 기능". ① `prearmLiveSeqPhaseDve()` 신설 — LIVE 시퀀스가 각 단계로 실제로 컷하기 "직후" 곧바로 다음 단계의 DVE 레이어를 미리 채워 넣고(지금 온에어 중인 DVE는 절대 건드리지 않는 가드 포함, updatePvwLookaheadVMix()의 curSrvName 가드와 동일 원리) PreviewInput으로 PVW에 올려 vMix 프리뷰 모니터로 미리 확인할 수 있게 함 — `startLiveSeqVMix()`(첫 질문 진입 시)·`advanceLiveSeq()`(다음 단계 진행마다) 양쪽에 연동. ② `resetLiveSeqUI()`(아이템 전환 시 selectItem()이 항상 호출하는 지점, 자막의 clearCGAutoTimers()와 같은 자리)가 이번 아이템이 실제로 썼던 DVE들을 레이어 "기본값"으로 되돌리도록 확장 — 자막과 달리 DVE 레이어는 끄는 개념이 없어 "기본값 복귀"로 같은 효과를 냄, 어떤 질문에서 재정의(phase.layers)해둔 값이 이후 그 DVE를 재사용하는 다른 상황에 남아있지 않게 함. ③ `applySourceLayers()`가 SetLayer 성공 여부만이 아니라 그 대상 인풋이 실제 vMix 인풋 목록에 진짜 존재하는지까지 매번 대조해 로그에 ✓/⚠ 표시(vMix가 존재하지 않는 인풋을 가리켜도 에러 없이 조용히 무시하는 이 프로젝트의 기존 교훈과 동일 이유) + 설정 탭에 독립 "🔍 DVE 레이어 구성 확인" 버튼·결과창 신설(verifyVMixInputMap()과 같은 패턴) — 이름·연결 존재까지만 자동 확인 가능하고, 실제 화면 배치(위치/크기)는 "지금 실제로 적용" 버튼으로 vMix 프리뷰 모니터에서 직접 봐야 한다고 명확히 안내 | Playwright(vmixCall/vmixGetState를 실제 인풋 목록이 있는 것처럼 가로채 목킹) — 온에어 중인 DVE는 프리암이 건드리지 않는 것 확인, 존재하지 않는 인풋(REAL_SRV1 제거)을 기본값으로 등록해뒀을 때 검증이 정확히 그 레이어만 ✗로 잡아내는 것 확인, 아이템 전환 시 실제 SetLayer 호출로 재정의값(SRV2)이 아니라 기본값(SRV1)으로 되돌아가는 것 확인, 설정 탭 확인 버튼의 실제 렌더링(전부 초록 ✓) 스크린샷으로 확인 |

| DVE `SetLayer` 파라미터 실장비 검증 — 처음 구현이 완전히 틀렸던 것 발견·수정 (07-18) | 사용자 요청으로 실제 vMix(192.0.2.2, v29.0.0.48, 방송 중인 라이브 시스템)에 직접 연결해 확인. **07-18 앞선 구현은 실제로는 전혀 동작하지 않는 파라미터였다** — `Input=<합성>&SelectedIndex=<idx>&Value=<대상>`은 vMix가 "Invalid Parameters"로 거부(HTTP 500, 함수 이름 자체는 인식하지만 파라미터가 틀림). vMix 공식 함수 레퍼런스(vmix.com/help29/ShortcutFunctionReference.html)를 직접 조회해 진짜 형식을 확인: `Function=SetLayer&Input=<합성>&Value=<레이어번호>,<새 소스>` — SelectedIndex 없이 Value 하나에 콤마로 합쳐 보낸다. 추가로 실측 전용으로만 드러난 두 가지: ① **레이어 번호가 1부터 시작**(Value="1,X"가 실제로는 XML의 `<overlay index="0">`를 바꿈, 이 프로젝트의 0부터 세는 데이터 모델과 어긋남) — `_vmixSetLayer()` 안에서만 +1 변환해 데이터 모델/UI는 그대로 0부터 유지. ② **존재하지 않는 대상을 가리키면 "성공"이라고 응답하면서 그 레이어를 통째로 지워버림**(XML에서 `<overlay index="N">` 자체가 사라짐 — 단순 무시가 아니라 파괴적 실패, 유효한 SetLayer를 같은 번호로 다시 걸면 복구는 됨을 확인). 이 발견에 따라 `applySourceLayers()`를 "먼저 보내고 나중에 확인"에서 "먼저 실제 인풋 목록과 대조해 존재할 때만 보낸다"로 방어 순서를 바꿈 — 검증 실패한 레이어는 SetLayer 자체를 호출하지 않아 직전 내용이 그대로 유지됨. 부수 검증: 제목(title) 기반 주소 지정도 정상 동작, `PreviewInput`을 레이어 구성 합성 인풋에 걸어도 PGM(active)엔 전혀 영향 없음(프리암 기능의 전제 확인) | **실제 vMix 라이브 시스템 직접 검증** — curl로 SetLayer 호출 전/후 XML 상태를 매번 재조회해 실제 변경 여부 확인(HTTP 응답 텍스트만으로는 신뢰 불가 재확인), 정상 케이스·범위 밖 레이어 번호·존재하지 않는 대상 3가지 모두 테스트, 테스트 중 실수로 다른 소스로 잘못 복원했던 것도 원래 key와 대조해 즉시 발견·재수정, 모든 테스트가 끝난 뒤 PGM(active=18)은 처음부터 끝까지 한 번도 안 바뀐 것과 PVW(preview)가 정확히 원래 값(22)으로 복구된 것까지 최종 확인 — **실제 방송에 영향 없이 완료** |

| DVE 상태 미리보기 — 시도했으나 vMix에 실제로 없는 API였음, 만들었다가 다시 제거 (07-18) | 사용자 요청으로 07-18에 `/proxy/vmix-snapshot?input=N`(vMix `/api/snapshot/{번호}.jpg`를 프록시)과 아이템 편집창 썸네일을 만들었으나(당시엔 vMix 하드웨어가 오프라인이라 목킹으로만 검증), 이후 사용자 요청 "실제 vMix로 테스트해서 확인해줘"로 실장비(192.0.2.2)에 직접 여러 경로로 요청한 결과 **`/api/snapshot/{번호}.jpg`는 vMix에 아예 존재하지 않는 엔드포인트였다**(항상 HTTP 404). vMix 공식 문서를 다시 조회해보니 애초에 `SnapshotInput` Function은 HTTP로 이미지를 돌려주는 게 아니라 **vMix PC 로컬 디스크에 파일로 저장**하는 기능이었다(파일명 안 주면 저장 대화상자가 뜨는 UI 동작) — 폴링용 썸네일에 처음부터 쓸 수 없는 API를 잘못 가정하고 만든 것. vMix 자체 내장 웹 컨트롤러(`/controller`)의 입력 버튼들도 실제 라이브 썸네일이 아니라 고정된 빈 아이콘만 쓰는 것을 확인해, vMix가 이 방식의 실시간 입력 미리보기 자체를 지원하지 않는다는 것까지 재확인했다. 사용자에게 상황을 알리고 처리 방법을 확인한 뒤(제거 후 기존 방식 안내로 되돌리기를 선택) `_dveSnapNumberCache`/`_dveSnapTimer`/`_vmixInputNumberFor`/`_refreshDveSnapThumb`/`_dveSnapError`/`_refreshAllDveSnapThumbs`/`startDveSnapPreview`/`stopDveSnapPreview`와 아이템 편집창의 썸네일 마크업, server.py의 `/proxy/vmix-snapshot` 엔드포인트를 전부 되돌렸다 — DVE 화면 확인은 기존대로 설정 탭 "지금 실제로 적용" 버튼으로 PVW에 걸어 vMix 프리뷰 모니터에서 직접 보는 방식(이쪽은 07-18 SetLayer 실장비 검증에서 이미 안전성까지 확인된 방식)이 유일한 방법으로 남는다 | **실제 vMix 라이브 시스템 직접 검증**(PGM=16, PVW=1인 실제 온에어 상태에서 진행) — curl로 여러 경로 변형 전부 시도해 전무 404 확인, vMix 공식 문서 재조회로 Function의 진짜 동작(파일 저장) 확인, `/controller` 페이지 HTML 직접 조회로 vMix 자체도 라이브 썸네일이 없다는 것 재확인, 테스트 전후 PGM(active=16)·PVW(preview=1) 완전히 불변 확인(GET/HTML 조회만 수행, Function 호출 없음) — **실제 방송에 영향 없이 완료, 기능은 되돌림** |

| MARS→Master 자막 전달 경로 재검증 + 아이템 편집창에 IN/OUT 타이밍 표시·수정 추가 (07-18) | 사용자 요청: "마스에서 자막을 수정해서 마스터로 보냈는데, 정확하게 전달이 되었는지 확인해줘" — 실제로 MARS에서 수동 타이밍 1건(IN+5s/OUT+20s)·영상 길이 기준 자동 타이밍 1건을 만들어 `toMasterCuesheet()`→`POST /api/cuesheet`(저장된 원본 JSON까지 직접 확인)→Master `pullCuesheetFromServer()`→`cueData.items[].cg`까지 전 구간을 실제로 재현해 종류·내용·초 단위 타이밍이 한 글자도 틀리지 않고 전달되는 것, 그리고 `describeCGPlan()`(실제 방송 스케줄링과 같은 계산 함수)이 그 값을 정확히 해석하는 것까지 확인 — 데이터 경로 자체는 정상. 다만 확인 과정에서 Master 아이템 편집창의 자막 목록(`renderCgRows`)이 종류·내용만 보여주고 IN/OUT 초는 화면에 전혀 표시하지 않는다는 것을 발견(데이터는 멀쩡히 보존되지만 편집창만 봐서는 타이밍 지정 여부를 알 수 없었음) — 사용자 요청으로 각 자막 행에 IN/OUT 초 입력칸 2개(비우면 "타이밍 없음=수동" — `computeCGPlan()`의 판정 기준과 동일 규칙)와 사람이 읽는 요약(`⏱ IN+5s / OUT+20s` 등)을 추가해 편집창에서 바로 보고 고칠 수 있게 함(`setCgRowTiming()`) | Playwright(실제 서버 기동, MARS→서버 저장 JSON 직접 조회→Master 로드까지 실제 함수 호출로 3단 대조 확인) + 새 UI는 수동/자동/미지정 3가지 케이스 모두 렌더링·값 수정(입력→즉시 draft 반영·요약 갱신)·빈 값으로 되돌리기까지 실제 조작으로 확인 |

| 프리롤/프레임 설정 — 기본값 저장/불러오기 (07-18) | 사용자 요청: "지금 프리롤 및 프레임 설정을 기본값으로 저장해줘" — 자막 설정(`setVMixCGConfigAsDefault`/`loadVMixCGConfigDefault`)과 완전히 같은 패턴을 프리롤(초)/조기전환(초)/프레임레이트/시작 후 프레임 건너뛰기/끝나기 전 정지 프레임에도 적용. "⭐ 지금 설정을 기본값으로"가 현재 화면 값을 먼저 실제 설정(`nbsClipTimingCfg`)으로 저장한 뒤 별도 슬롯(`nbsClipTimingCfgDefault`)에 스냅샷 복사, "↺ 기본값 불러오기"가 그 스냅샷으로 복원 — 실제 vMix/네트워크 환경으로 반복 테스트해서 이 PC에 맞는 값을 찾아낸 뒤, 나중에 값을 잘못 건드리거나 다른 PC로 옮겼을 때 원클릭 복구 가능 | Playwright(실제 함수 호출) — 임의 값(프리롤 1.5s/시작스킵 8프레임/끝정지 20프레임)을 입력→기본값 저장→다른 값으로 망가뜨림→불러오기로 정확히 원래 값 복원까지 확인 |

| 조기전환/끝나기 전 정지 — 여유(margin) 실시간 표시 (07-18) | 사용자 보고("디졸브에 영향이 생기는 부분이 프레임 설정이 영향을 줄 수 있는데") — 코드 추적 결과, "조기전환(초)"이 거는 `scheduleClipEarlyAdvance()`(디졸브 등 실제 전환을 시작시키는 `nextCue()`)와 "끝나기(프레임) 전 정지"가 거는 `scheduleServerClipAutoStop()`(안전망 `Function=Pause`)이 같은 서버 클립을 두고 경쟁하는 독립 타이머라는 것을 확인. `_vmixCut()`이 디졸브 유예(`srvActiveSetAt`)를 실제로 세우는 건 vMix로 전환 명령을 보내는 네트워크 왕복이 끝난 "뒤"라, 두 시점 사이 여유가 이 프로젝트에서 실측된 지연 폭(수백 ms)보다 얇으면 안전망이 먼저 발동해 디졸브 도중 화면이 멈출 수 있다 — 사용자의 실제 값(조기전환 0.5초/끝나기 전 정지 5프레임/15fps)으로 계산해보니 여유가 167ms로 위험 기준(300ms, `_vmixCut()`의 디졸브 유예 안전 여백과 동일 기준)보다 얇은 것을 확인. 세 입력칸 바로 아래 실시간 계산 표시(`renderClipTimingMarginInfo()`, 값 바뀔 때마다 자동 갱신)를 추가해 여유가 300ms 이상(초록·충분함)/0~300ms(주황·얇음, 권장 조기전환 값까지 제시)/0 이하(빨강·위험) 세 단계로 바로 보이게 함 | Playwright(실제 함수 호출) — 사용자 값(0.5s/5프레임/15fps→167ms 얇음), 기본값(2s/15프레임/30fps→1500ms 충분함), 조기전환 0(계산 대상 아님 안내) 세 케이스 모두 정확한 계산·색상·문구로 표시되는 것 확인 |

| 빌드(버전) 표시 — 이미 있었지만 사실상 안 보이던 것을 헤더 앞쪽으로 이동 (07-18) | 사용자 요청: "마스터와 마스에 현재 버전을 표시해줘, 특히 날짜와 시간 표시도 해줘" — 확인해보니 `#build-stamp`(파일 실제 수정시각을 "빌드 YYYY-MM-DD HH:MM"로 표시, 07-06에 이미 구현됨)는 이미 있었지만 Master는 툴바 맨 끝(vMix/Videohub 연결 버튼·시계 뒤)에, MARS는 아이콘 툴바·파일 입력 뒤에 9px 회색 글자로 붙어있어 실측 뷰포트에서 오른쪽 끝에 겨우 걸치거나 다른 요소에 묻혀 실질적으로 안 보이는 위치였다(사용자가 "표시해달라"고 다시 요청한 이유로 추정) — DOM/렌더링 자체는 정상이었으므로 코드가 고장난 게 아니라 위치 문제. Master는 로고("Master") 바로 옆, MARS는 상태뱃지("B") 바로 옆으로 옮기고 11px로 살짝 키움 — 둘 다 항상 맨 앞쪽에 있어 창 너비와 무관하게 항상 보임 | Playwright(실제 로드) — 이동 전 좌표 확인(뷰포트 경계에 거의 닿아있었음) → 이동 후 두 앱 모두 로고/뱃지 바로 옆에서 정상 표시("빌드 2026-07-18 HH:MM" 형식으로 날짜+시간 모두 포함), 스크린샷으로 실제 안 잘리고 보이는 것까지 확인 |

| 카메라 선택 무시 버그 — 타입 기본 카메라(rule.cams[0])가 아이템의 실제 선택을 항상 덮어씀 (07-18) | 사용자 보고: "마지막 앵커는 cam2로 설정했는데, 왜 cam1로 클로징이 되지?" — 코드 추적 결과 카메라 소스를 계산하는 5곳(`_resolveCameraSrcId`/`startAnchorPhaseVMix`/`updatePvwLookaheadVMix`/진행 미리보기 라벨/`updateMonitor`)이 전부 `rule.cams && rule.cams[0]`(그 타입의 기본 카메라)를 `item.sources`(아이템 편집창에서 실제로 체크한 카메라)보다 먼저 확인하고 있었다 — 아이템 편집창은 타입과 무관하게 CAM1~4를 전부 체크할 수 있는데도, 실제 온에어·PVW 예고·진행 미리보기 텍스트 전부 "이 타입 기본값이 있으면 무조건 그것"으로 조용히 나갔던 것. CLOSING의 기본 카메라가 cam1이라(`cams:['cam1','cam2']`), 아이템에서 cam2만 체크해도 실제로는 항상 cam1로 컷됐다. 5곳이 각자 같은 공식을 따로 베껴 쓰고 있어 이런 어긋남이 생겼던 것이므로, 공용 함수 `_itemCamSrcId(item, rule)`(item.sources의 카메라 우선, 없을 때만 rule.cams[0])를 하나 만들어 5곳 전부 이걸로 교체 — 앞으로는 한 곳만 고치면 전부 같이 맞는다 | Playwright(실제 함수 호출 + vmixCall 목킹) — `_resolveCameraSrcId`/`_itemCamSrcId` 단위 테스트 4케이스(CLOSING을 cam2로 명시 지정→cam2, 아무것도 안 지정→기본값 cam1, ANCHOR를 cam1로 오버라이드→cam1, 아무것도 안 지정→기본값 cam2) 모두 정확, 실제 `applyItemToVMix()`/`startAnchorPhaseVMix()` 호출로 진짜 vmixCall 요청 문자열까지 확인(`Input=카메라2`/`Input=카메라1`로 각각 정확히 나감), 진행 미리보기 라벨("앵커: CAM1")도 실제와 일치 |

| 디졸브 도중 멈춤 — 네트워크 왕복 경합 자체를 제거(설정값에 의존하지 않는 근본 수정) (07-18) | 사용자 보고: "홈플러스 마지막단신은 지금 프레임 설정에서 디졸브가 되다가 멈추는 현상" — 직전에 발견한 "조기전환/끝나기 전 정지 여유 167ms" 위험이 실제로 재현된 것으로 보고, 설정값을 늘리라고 안내하는 것에서 나아가 근본 원인 자체를 제거함. `_vmixCut()`이 디졸브(Mix/Fade/Wipe) 유예(`srvActiveSetAt`)를 세우는 시점이 지금까지 `await vmixCall(...)`(vMix로 전환 명령을 보내는 실제 네트워크 왕복) "이후"였다 — 그 왕복 시간 동안은 유예가 아직 안 걸린 상태라, 0.1초 간격으로 도는 `scheduleServerClipAutoStop()`의 정밀 정지 폴링이 그 틈에 먼저 목표 지점에 도달하면 디졸브가 시작되기도 전에(또는 도중에) `Function=Pause`가 걸려 화면이 멈출 수 있었다 — 설정된 여유가 넉넉해도 결국 "네트워크 왕복 시간"이라는, 설정으로 통제 불가능한 변수가 껴 있었던 것. 유예를 vmixCall() "전에" 먼저 세우도록 순서를 바꿔, 전환 명령을 보내는 순간부터 이미 안전장치가 얌전해지게 함 — 여유 설정값의 크기와 무관하게 이 경합 창 자체가 사라진다 | Playwright(실제 함수 호출) — vmixCall에 실제 네트워크 지연을 흉내낸 300ms 지연 응답을 걸어두고, 응답이 오기 전(10ms 시점)에 `srvActiveSetAt`을 확인해 이미 미래 시각으로 설정돼 있는 것을 확인(수정 전이었다면 응답이 온 뒤에야 설정됐을 것) |

| 출연 아이템 DVE 화면이 안 바뀌는 문제 — 코드는 정상 동작, 설정 누락 시 완전히 무음이었던 게 원인 (07-18) | 사용자 보고: "출연아이템에서 DVE 화면이 설정한 내용과 다르게 변경이 되지 않는데, DVE 소스 설정은 변경이 어렵나?" — DVE1/DVE2에 실제 레이어·vMix 인풋명을 전부 갖춘 상태로 출연(guest) 시퀀스 전체(앵커 갈라치기→기자→서버 갈라치기)를 실제 함수 호출로 재현한 결과, `SetLayer` 호출이 정확한 값(`Value=1,카메라2`/`Value=2,기자원샷` 등)으로 정상 발사되는 것을 확인 — 코드 경로 자체는 문제 없음. 근본 원인은 `switcherSources`의 DVE1/2/3가 **기본값에 `layers` 배열 자체가 없다는 것**(설정 탭에서 최소 1회 "+ 레이어 추가"로 직접 등록해야 함) — 그리고 `applySourceLayers()`가 `layers.length===0`이면 완전히 조용히(로그 한 줄도 없이) 아무 것도 안 하고 반환하고 있어서, 이 등록을 안 한 채 방송을 돌리면 매 단계 전환마다 아무 일도 안 일어나는데 그 사실을 알 방법이 어디에도 없었다("설정한 내용과 다르게 안 바뀐다"는 증상만 남고 원인 단서가 전무). id가 `dve`로 시작하는 소스가 레이어 0개인 채로 `applySourceLayers()`가 호출되면 경고 로그를 남기도록 추가(일반 카메라/서버처럼 원래 레이어가 없는 게 정상인 소스는 대상에서 제외해 소음 방지) | Playwright(실제 함수 호출) — DVE1/2 레이어·인풋명 전부 채운 상태로 `startLiveSeqVMix`→`advanceLiveSeq` 3단계를 실제로 진행해 매 단계 정확한 SetLayer 값이 나가는 것 확인(코드 정상 재확인), 별도로 레이어 미등록 상태에서 `applySourceLayers('dve1')` 호출 시 경고 로그가 뜨고 `cam1`/`rs1`(레이어 없는 게 정상인 소스) 호출 시엔 안 뜨는 것까지 확인 |

| DVE 레이어 원격 제어 — 실제 vMix 라이브 시스템 재검증 (07-18, 사용자 요청 "네가 먼저 테스트를 해봐") | 사용자가 설정 탭에서 직접 레이어를 등록한 뒤 "먼저 테스트해보라"고 요청 — 사용자의 실제 설정값은 브라우저 localStorage에 있어 이쪽에서 볼 수 없으므로, 대신 실제 vMix(192.0.2.2, 방송 중, PGM=input22/PVW=input10)에 이미 존재하는 진짜 합성 인풋 `right-gala`(온에어·프리뷰 어디에도 없는 완전히 유휴 상태, 안전)를 대상으로 두 단계로 재검증: ① curl로 SetLayer를 직접 걸어 실제 오버레이 키가 정확히 바뀌는 것을 원본 XML 재조회로 확인 ② Master의 실제 코드 경로(`applySourceLayers()` — "▶ 지금 기본값 적용" 버튼과 동일 함수)를 그대로 호출해(임시로 미사용 슬롯 dve3에 `right-gala`와 같은 레이어 구성을 맞춰 넣음) 똑같은 결과가 나오는 것을 독립적인 curl 재조회로 재확인 — Master UI 버튼→실제 vMix까지 전 구간이 지금 이 순간에도 정상 동작함을 재확인. 부수 발견: vMix의 SetLayer는 원래 레이어(오버레이)가 하나도 없던 일반 인풋에도 새 오버레이를 그냥 만들어버린다(실측 확인, 즉시 정리) — "레이어"가 DVE류 소스에만 국한된 게 아니라 기술적으로는 어느 소스에나 적용되지만, vMix에서 미리 위치·크기를 배치해둔 진짜 합성 인풋이 아니면 화면 전체를 덮어씌우는 형태로만 생겨 실용적 의미가 없다는 것도 함께 확인 | **실제 vMix 라이브 시스템 직접 검증** — 매 단계마다 변경 전/후 XML을 재조회해 실제 오버레이 키 변경을 확인(HTTP 200 응답만으로는 신뢰 안 함, 기존 확립된 방식과 동일), 테스트 종료 후 정확히 원래 키로 복구된 것과 PGM(active=22)·PVW(preview=10)가 테스트 시작부터 끝까지 단 한 번도 안 바뀐 것까지 최종 확인 — **실제 방송에 영향 없이 완료** |

| 아이템 편집창에 DVE 레이어 설명 추가 + 사용자 매뉴얼 갱신 (07-18) | 사용자 요청: "출연/중계 아이템 편집창에도 이 설명 안내문구 추가해줘" + "이걸 메뉴얼에도 그림과 함께 넣어줘" — 위 재검증에서 확인한 "레이어는 vMix에 미리 화면분할을 짜둔 소스에만 의미 있다"는 설명을 ① 중계 시퀀스 힌트 텍스트(`ie-liveseq-hint`)에 추가 ② 갈라치기 단계의 소스에 레이어가 하나도 없을 때(`_liveSeqPhaseLayerRow`) 예전엔 아무 것도 안 보여주던 것을 "이 소스는 등록된 레이어가 없습니다 — 화면분할 구도라면 설정 탭에서 먼저 레이어를 등록하세요. 일반 카메라/서버라면 정상입니다" 안내로 바꿈(dve*로 이름을 제한하지 않고 소스가 선택된 모든 경우에 사실 그대로 안내) — 편집 단계에서부터 바로 확인 가능하게 함. `user-guide/item-editor-guide.html` 2절(중계/출연 시퀀스)에도 실제 화면 캡처 2장(①DVE2 레이어 미등록·DVE1 레이어 등록됨을 나란히 비교, ②PDF/설정 탭의 레이어 등록 화면)과 함께 새 소단원 추가 | Playwright(실제 함수 호출) — 레이어 등록/미등록 두 단계를 한 아이템에 같이 넣어 편집창에서 각각 정확한 안내가 뜨는 것 확인, 매뉴얼은 실제 로드 후 새로 추가한 이미지 2장 모두 정상 렌더링(`naturalWidth` 확인) 및 스크린샷으로 레이아웃까지 확인 |

| 서버 소재가 엉뚱한 다음 소재로 넘어가는 문제 — 정식 추적 밖 재생 경로 전부에 남아있던 공백 발견·중앙 수정 (07-18) | 사용자 보고: "서버 소재가 재생되고 어떤 것은 다음 소재로 넘어가던데, 잘 체크해줘" — `_vmixPollInputName`(뉴스 큐시트 클립 큐가 "이 서버는 지금 내가 추적 중"이라고 표시해두는 전역 변수)이 라디오 "등록된 영상"에서는 이미 무효화 처리가 돼 있었지만(`playRadioMediaItem`, 이전 세션에 먼저 발견·수정), 같은 서버를 재생하는 다른 경로들 — MARS 제작 큐시트(VMU)의 `applyLiveItemToVMix`, 설정 탭 "vMix 리스트 테스트"의 `testPlayVMixListItem` — 에는 이 무효화가 빠져 있었다. 재현 조건: 뉴스 큐시트에서 멀티클립 아이템이 SRV1을 재생 중(다음 클립이 남아있음) → VMU 카드나 테스트 버튼으로 같은 SRV1에 완전히 다른 파일을 재생 → 그 파일이 자연 종료(Completed)되면 `_vmixPollTick()`의 "미디어 종료 감지"가 이걸 그 옛 뉴스 아이템의 "다음 클립"으로 착각해 엉뚱한 뉴스 클립으로 넘어가버림("서버 소재가 재생되고 어떤 것은 다음 소재로 넘어간다"와 정확히 일치). 호출부마다 따로 챙기는 대신, 모든 재생 경로가 공통으로 거치는 최하위 함수 `_vmixArmFromList()` 시작 지점 한 곳에서 "지금 이 서버를 점유하는 순간 그 서버에 걸린 낡은 추적은 무조건 무효화"하도록 중앙화 — 앞으로 새 재생 경로가 추가돼도 이 지점만 거치면 자동으로 안전하다 | Playwright(실제 함수 호출) — 뉴스 큐시트 멀티클립 추적이 걸린 상태에서 VMU 재생(`applyLiveItemToVMix`)과 설정 탭 테스트 재생(`testPlayVMixListItem`) 양쪽 모두 낡은 추적이 정확히 무효화되는 것 확인, 정식 뉴스 큐시트 흐름(단일 클립 아이템 재생)은 여전히 정상적으로 추적이 다시 걸리는 것까지 확인해 회귀 없음 검증 |

| MARS 출연('출') 코드가 중계 시퀀스 대신 브리지로 나가는 문제 재발 — 자기 치유 + 설정 화면 잠금 (07-18) | 사용자 보고: "마스터에서 출연은 중계가 아니라 출연으로 표시해줘" → "브리지라고 되어있군" — 이전 세션에 이미 한 번 발견·수정했던 "'출'/'중' 코드가 defaults에 없으면 조용히 BRIDGE로 폴백" 버그가 재발한 것으로 확인. 원인: `loadTypeMapping()`이 저장된 매핑을 불러올 때 `{...defaults, ...saved}`로 병합하는데, 이건 "사용자가 의도적으로 바꾼 값은 존중한다"는 원칙상 맞는 동작이지만 — '출'/'중'→LIVE는 사용자가 자유롭게 고를 수 있는 취향이 아니라 Master 실행 엔진이 의존하는 고정 상수였다. 예전 어느 시점에 저장된 값이 BRIDGE였다면(구버전 기본값이거나 설정 화면에서 실수로 선택) 그 뒤로 영원히 BRIDGE로 남는다. Cut/Mix 필수 프리셋과 같은 원칙 적용: `loadTypeMapping()`이 저장된 값과 무관하게 '출'/'중'을 항상 LIVE로 강제(자기 치유, 고쳐졌으면 콘솔 경고), 설정 화면("타입 설정" → NPS 코드 매핑)에서도 이 두 코드만 자유 선택 `<select>` 대신 "LIVE (고정 — 변경 불가)"로 잠가 재발 원천 차단 | Playwright(실제 함수 호출) — localStorage에 '출':'BRIDGE'로 손상된 매핑을 직접 심어두고 페이지를 새로 로드해도 `TYPE_MAPPING.direct['출']`이 LIVE로 자동 복구되는 것과 콘솔 경고가 뜨는 것 확인, `resolveMasterType()`으로 실제 변환 결과도 LIVE 확인, 설정 화면에서 출/중 행이 잠긴 표시로 나오고 나머지 코드(타/단/완 등)는 그대로 자유 선택 가능한 것까지 확인 |

| 리포트/단신/타이틀은 기타 소스를 쓰지 않는 게 기본 (07-18) | 사용자 요청: "일반 리포트와 단신, 타이틀, 서버 타이틀은 기타소스에 사용하지 않음으로 해줘. 기본이 그러니 잘 체크해줘"("서버 타이틀"은 TITLE과 같은 뜻으로 확인) — 이 세 타입은 카메라·서버로만 진행되는 형식이라 "DVE·중계 회선" 개념의 기타 소스가 원래 필요 없는데, 예전엔 아이템이 중계(LIVE) 등 기타 소스를 쓰는 타입이었다가 이 타입들로 바뀌어도 그 값이 그대로 남아있어 편집창에 혼란을 남기는 데이터 위생 문제가 있었다. `onItemEditorTypeChange()`에 REPORT/ANCHOR/TITLE로 전환 시 기존 기타 소스를 자동으로 비우는 로직 추가(뭔가 지워졌을 때만 안내 토스트, 이미 비어있으면 조용히 통과) — 카메라 기본값 재설정처럼 확인창을 거치지 않고 바로 적용(이 세 타입엔 기타 소스가 "절대 있으면 안 되는" 값이라는 사용자 의도로 해석) | Playwright(실제 함수 호출) — LIVE(dve1 기타소스)→REPORT 전환 시 비워짐, BRIDGE(rs2)→TITLE 전환 시 비워지고 토스트 1회만 발생, 이미 비어있는 상태에서 같은 타입으로 재호출해도 중복 토스트 없음, BRIDGE처럼 대상에 없는 타입은 기타 소스가 유지되는 것까지 확인 |

| UI 정리 — 설정형 UI 접기(진행 패널 영상 재생 옵션 + PDF/설정 탭 전 섹션) (07-19) | 사용자 요청: "자주 사용하지 않는 UI는 접기 기능을 만들거나 체크 기능을 통해서 UI상 정리해줘" — ① 중앙 진행 패널에서 항상 펼쳐져 있던 영상 재생 옵션 블록(처음부터 재생/자동 진행 체크박스 + 프리롤/조기전환/프레임 설정 + 기본값 저장/불러오기, 화면의 큰 부분을 차지하지만 한 번 맞춰두면 방송 중엔 안 건드리는 설정형 UI)을 `<details>` 접기로 전환 — 기본 접힘이되 요약 줄에 지금 값 전체("프리롤 1.5s · 조기전환 0.5s · 15fps · ...·자동진행 켜짐")가 항상 보여 접힌 채로도 상태 확인 가능(`renderClipTimingSummary()`), 펼침 상태는 localStorage 유지. ② PDF/설정 탭의 15개 섹션 전부를 제목 클릭으로 접고 펼 수 있게 함(`initSettingsCollapse()`) — HTML을 섹션마다 감싸지 않고 로드 시 `.sec-title` 기준으로 한 번 후처리하는 방식이라 섹션이 추가돼도 자동 적용, 기본 접힘이라 설정 탭이 목차처럼 보이고 자주 쓰는 섹션은 펼쳐두면 유지(제목 텍스트 기준 localStorage). 라이브 운영 탭(스위처/CG/미디어)은 방송 중 실수로 접혀 있으면 위험해 의도적으로 제외. 구현 중 실버그 1건 발견·수정: 형제 순회가 여러 섹션을 감싼 래퍼(`#vmix-settings-section`)를 직전 섹션의 내용으로 오인해 섹션 10여 개가 통째로 사라졌음 — 래퍼(내부에 .sec-title 포함) 앞에서도 멈추도록 수정 | Playwright(실제 로드·클릭) — 진행 패널 접기 기본 접힘+요약 줄 정확, 설정 탭 15개 섹션 전부 접힘 화살표 표시·클릭 펼침·내용 실렌더링(스위처 소스 관리 32행) 확인, 두 접기 모두 새로고침 후 상태 유지, 콘솔 에러 0건, 스크린샷으로 최종 레이아웃 확인 |

| 회귀 전수 점검 — 수동 재생 경로의 안전장치 미등록 구멍 2곳 발견·수정 (07-19) | 사용자 요청: "지금까지 있었던 문제를 수정하면서 혹시나 다시 나타날 수 있는 부분을 면밀히 검토해서 수정해줘" — 이번 세션에 고친 버그들과 같은 계열의 문제가 남아있을 만한 지점을 전수 점검. 서버 클립을 재생시키는 모든 코드 경로를 추적한 결과, **수동 조작 경로 2곳이 안전장치(srvActiveClipIdx) 등록 없이 재생만 시작하고 끝나는** 것을 발견: ① 스위처 버튼/라디오 라이브 컷으로 서버를 PGM에 수동 컷할 때(`setPGM`/`radioCutTo`→`_resumeOrRestartIfClip`) ② 서버 카드의 ▶ 재생 버튼(`mediaCtrlSrv`) — 둘 다 그 클립이 자연 종료될 때 아무도 안 지켜보고 있어 vMix "Auto Next"가 리스트의 다음 항목(엉뚱한 다음 소재)을 그대로 재생할 수 있었다("서버 소재가 어떤 것은 다음 소재로 넘어간다"의 수동 조작 케이스 — 자동 진행 경로들은 이미 전부 등록하고 있었음). 공용 헬퍼 `_registerManualClipPlay()`를 만들어 두 경로 모두 재생 시작 시 현재 선택 인덱스로 안전장치에 등록 + 자동정지 예약 — 단 뉴스 큐시트의 클립 큐가 그 인풋을 정식 추적 중이면(멀티클립 진행 중 PD가 같은 서버를 수동으로 다시 잡는 관행) 건드리지 않는다(여기서 auto-stop을 걸면 Completed 대신 Paused로 끝나 큐의 다음 클립 전환이 영영 안 걸림). 그 외 점검 결과: 전환 유예(srvActiveSetAt)는 3개 안전장치 모두 일관되게 확인 중, `cutToVideoVMix`/`applyItemToVMix`/라디오 경로는 전부 정상 등록, `doAuto()`는 리스트 인덱스를 바꾸지 않아 해당 경합 없음 — 추가 수정 불요 판단 | Playwright(vMix 목킹, 실제 함수 호출) — 수동 컷(`_resumeOrRestartIfClip`) 후 선택 인덱스가 정확히 등록되는 것, 정식 클립 큐 추적 중엔 건드리지 않는 것, 서버 카드 ▶ 버튼도 등록되는 것 3케이스 모두 확인 |

| DVE 레이어 안 서버 소재가 재생되지 않고 정지 화면만 나오는 문제 (07-19) | 사용자 보고: "중계, 출연에 있는 DVE의 서버 소재가 내가 설정한 소재로 제대로 재생이 되지 않고 있어" — `applySourceLayers()`(vMix `SetLayer`)는 DVE 레이어에 "어느 인풋이 보일지"만 바꿀 뿐 그 인풋의 재생 자체를 시작시키지 않는데, 서버(SRV1~3)는 PGM에서 내려갈 때마다 자동으로 처음(0초)으로 되감아 정지해두는 게 이 프로젝트의 표준 동작이라(`_vmixPollTick`), 중계/출연 DVE의 "서버 갈라치기" 레이어에 SRV를 배치해도 마지막으로 정지해둔 프레임만 그대로 보일 뿐 실제로는 재생되지 않았던 것이 원인. `advanceLiveSeq()`가 DVE 단계로 컷하기 직전에 그 레이어에 배치된 서버들을 실제로 재생(Restart+Play)시키는 `playDveLayerServers()`를 신설해 연동, 다음 단계로 넘어가거나(`advanceLiveSeq`) 아이템이 끝날 때(`resetLiveSeqUI`)는 화면 밖에서 계속 재생되지 않도록 `stopDveLayerServers()`로 정지 — 프리뷰 단계(`prearmLiveSeqPhaseDve`)는 기존처럼 재생시키지 않음(온에어 전 미리 재생 방지, 표준 srv 프리암 규칙과 동일) | Playwright(vmixCall을 가로채 실제 발사되는 함수 호출 순서 확인) — SPLIT-S(dve1, 레이어 서버=srv1) 단계 진입 시 `SetLayer→Restart→Play→Cut` 순서로 실제 재생이 시작되는 것, 다음 단계(카메라)로 넘어갈 때 `Pause→Restart`로 그 서버가 정지되는 것까지 실제 함수 호출로 확인 |

| 위 수정 — 실제 vMix 라이브 시스템 재검증 (07-19) | 실장비(192.0.2.2, v29.0.0.48, PGM=DESK MAN ANCOR/PVW=미디어플레이어2인 실제 온에어 상태)에 Master가 이미 연결된 상태로 접속해, 실제 방송 서버(미디어플레이어1/2)나 실제 DVE 합성 인풋(`right-gala`/`left-gala` — 이미 서버 레이어를 프로덕션 용도로 물고 있어 건드리면 위험)은 손대지 않고, 완전히 유휴 상태인 예비 서버 인풋 `미디어플레이어 예비`(온에어·프리뷰·다른 레이어 어디에도 안 걸림)를 가리키는 임시 스크래치 소스(`localStorage`에 저장하지 않는 메모리 전용)로 `playDveLayerServers()`/`stopDveLayerServers()`를 직접 호출해 검증. 결과: 호출 직전 `Paused@80047`(기존 상태) → `playDveLayerServers()` 직후 `Running@0` → 1.5초 뒤 `Running@1520` → 3.0초 뒤 `Running@3040`(실시간과 정확히 비례해 위치가 늘어남 = 진짜 재생 확인, 상태 플래그만 바뀐 게 아님) → `stopDveLayerServers()` 후 `Paused@0`. 전 과정에서 PGM(active=1)·PVW(preview=16)는 curl 재조회로 단 한 번도 안 바뀐 것까지 확인 | **실제 vMix 라이브 시스템 직접 검증 — 실제 방송에 영향 없이 완료.** 브라우저 fetch 응답이 캐시될 수 있어 첫 시도에서 `cache:'no-store'` 없이는 상태가 안 바뀐 것처럼 잘못 보였던 것도 발견해 재검증 방식 자체를 고쳐 확인(브라우저 fetch와 별개로 서버 밖 bash curl로도 최종 상태 재확인) |

| 출연(guest) 시퀀스도 같은 방식으로 실장비 재검증 (07-19) | 사용자 요청: "출연 아이템도 실장비로 같은 방식으로 테스트해줘". 두 단계로 진행: ① (무위험, vmixCall 목킹) `liveSeq.kind:'guest'`로 실제 `computeLivePhases()`→`startLiveSeqVMix`→`advanceLiveSeq()` 전체를 7단계(질문2개) 끝까지 실행해, 라벨이 "앵커 갈라치기(DVE2)"·"기자"·"기자 서버 갈라치기(DVE1)"×2·"Full Shot"으로 정확히 나오고 DVE1 서버 갈라치기 진입마다 `SetLayer→...→Restart&Play(서버)→Cut(DVE1)`, 다음 단계로 넘어갈 때마다 `Pause→Restart(서버)`가 정확한 순서로 발사되는 것 확인 — 라벨/순서 모두 정상. ② (실장비) 이 세션 시작 시점엔 PGM=DESK MAN ANCOR/PVW=미디어플레이어2였던 것과 달리, 이번엔 접속해보니 **PGM=left-gala/PVW=RS1로 실제 방송이 진행 중**이었음 — 그래서 dve1의 실제 vMix 인풋명은 전혀 건드리지 않고(비어있는 채로 둠, `playDveLayerServers()`는 DVE 자신의 인풋명을 안 씀) `.layers`만 메모리에서 임시로 채워, 이 방송국의 실제 SRV3(`미디어플레이어 예비`, 온에어·PVW·다른 합성 인풋 어디에도 안 걸린 유휴 상태)를 대상으로 `playDveLayerServers('dve1', null)`/`stopDveLayerServers()`를 직접 호출 — `Paused@0`→재생 직후 `Running@20`→+1.5초 `Running@1520`→+3.0초 `Running@3040`→정지 후 `Paused@0`까지 실측 확인(SetLayer 자체는 이번엔 호출하지 않음 — 실제 합성 인풋을 하나도 안전하게 쓸 수 없는 상태였으므로 범위에서 제외). **테스트 도중(3.5초 사이) PGM/PVW가 active=11(left-gala)/preview=18(RS1)에서 active=18(RS1)/preview=11(left-gala)로 실제로 바뀐 것을 발견** — 이 세션의 호출은 전부 `미디어플레이어 예비`(입력 7)만 대상으로 했고 input 11/18은 단 한 번도 호출하지 않았으므로 이 전환은 테스트가 만든 게 아니라 실제 방송 진행자가 그 순간 실제로 전환을 진행했다는 뜻 — 실사용 중인 시스템에 접속해 테스트하고 있었다는 확인 | ① vMix 목킹(실제 함수 호출) — 7단계 전체 라벨·호출 순서 정확 확인. ② **실제 vMix 라이브 시스템 직접 검증** — 유휴 SRV3만 조작, 재생/정지 실측 확인, bash curl 재조회로 최종 상태 재확인(SRV3는 Paused@0로 깨끗하게 종료). ⚠️ 테스트 도중 실제 방송 전환이 관측됨(테스트가 원인 아님, 시스템이 실사용 중이었다는 의미) — 다음 세션 시작 시 사용자에게 확인 필요 |

| DVE 레이어 수정 이후에도 "재생 안 됨" 재발 — 근본 원인은 DVE가 아니라 "바로 이 서버 자체" 문제였음 (07-19) | 사용자 재보고: "아직도 출연, 중계에 매칭된 서버소재가 재생되지 않고, 바로 전에 재생했던 내용이 그대로 재생이 되고 있어." 실제 저장된 큐시트(`shared-cuesheets/latest.json` 등)를 직접 열어 진짜 원인 재조사 — 지금 이 방송국의 실제 출연/중계 아이템은 **DVE(화면분할) 자체를 전혀 안 쓰고** `splitASrc`/`liveSrc`/`splitSSrc`에 카메라·RS를 직접 배정해뒀고, 과거(07-16) 한 테스트 아이템은 `liveSrc:'srv3'`/`splitSSrc:'srv1'`처럼 **서버를 DVE 레이어 없이 그 자체로 바로** 배정한 적도 있었다 — 즉 바로 앞의 수정(`playDveLayerServers`)은 "DVE 레이어 안에 든 서버"만 처리했지, "이 시퀀스 단계 자체가 바로 서버"인 훨씬 흔한 경우는 전혀 다루지 않았던 것이 재발 원인. 이 경우 `_vmixCut()`은 그냥 전환만 걸 뿐 그 서버에 SelectIndex/Restart/Play를 전혀 안 하므로, 컷은 되어도 그 서버 자신은 "바로 전에 재생/정지해뒀던 내용"을 그대로 보여준다 — 사용자가 재현한 증상과 정확히 일치. `playDveLayerServers(srcId, overrides)`를 확장해 DVE 레이어 내부 서버뿐 아니라 **phase.src 자기 자신이 서버(`srv`로 시작)인 경우까지** 함께 Restart+Play시키도록 수정(레이어 유무와 무관하게 이 시퀀스가 쓰는 모든 서버를 대상으로) | Playwright(vmixCall 목킹, 실제 저장된 아이템 설정 그대로 재현: `splitASrc:'cam2'/liveSrc:'srv3'/splitSSrc:'srv1'`, DVE 전혀 없음) — 4단계(앵커갈라치기→중계풀샷→서버갈라치기→마무리) 전부 서버로 진입할 때마다 `Restart→Play→Cut`, 나갈 때마다 `Pause→Restart`가 정확히 발사되는 것 확인. **실제 vMix 라이브 시스템 직접 검증** — DVE를 아예 거치지 않고 `playDveLayerServers('srv3', null)`을 유휴 SRV3(`미디어플레이어 예비`)에 직접 호출 → `Paused@0→Running@0→Running@1500`(재생 확인)→정지 후 `Paused@0`, PGM/PVW(active=17/preview=9) 불변까지 bash curl로 재확인 — 실제 방송에 영향 없이 완료 |

| 위 수정 — 실제 저장된 큐시트로 방송을 한 번 실제로 돌려 최종 확인 (07-19) | 사용자 요청: "실제 큐시트로 방송 한 번 돌려서 최종 확인해줘". 진행 전 사용자에게 지금 이 vMix가 실제 방송 중인지 먼저 확인 요청 — "안전한 테스트/리허설 상태"라고 확답받은 뒤 진행. 실제 서버 저장 큐시트(`/api/cuesheet`, "새로운 큐시트 디졸부 테스트용")를 Master의 실제 "📡 서버 불러오기" 경로로 로드 → id 151 "(중계) 장마피해 속보" 아이템의 `splitSSrc`를 저장된 값(`rs1`, 서버 아님)에서 **테스트 목적으로만 메모리에서** `srv3`(유휴 예비서버)로 일시 변경(저장/서버 반영 없음) → 실제 "GO" 버튼과 동일한 `goToItem()`으로 이 아이템을 직접 온에어 진입시키고, "▶ 다음 단계"와 동일한 `advanceLiveSeq()`를 4단계(앵커갈라치기→중계풀샷→**서버갈라치기**→마무리) 끝까지 실제로 진행 — "서버 갈라치기" 단계에서 실제 PGM이 `미디어플레이어 예비`로 바뀌었고, bash curl로 0.5~1초 간격 3회 재조회한 결과 `position`이 6100→7260→8320ms로 실시간과 정확히 비례해 늘어나는 것을 확인(진짜 재생, 정지화면 아님) — **보고된 버그가 실제 GO 버튼·실제 시퀀스 엔진·실제 vMix로 완전히 재현·해결됨을 최종 확인**. 부수 발견 2건: ① "중계 마무리" 단계(`rs1`)가 무반응이라 조사한 결과 이 방송국의 `rs1` 스위처 소스는 `vmixInput`이 애초에 빈 값(미설정)이었음 — 내 수정과 무관한 기존 설정 공백, "소스 미설정, 화면 유지"로 안전하게 처리된 것 확인. ② **지난 두 세션에서 "내 조작과 무관하게 PGM/PVW가 바뀌는 걸 봐서 실제 방송 중인 것 같다"고 보고했던 것은 정정 필요** — 이번에 `cam2`(`GT-title1.gtzip`)로 컷을 걸었는데 실제 vMix 인풋 목록 23개를 전수 대조해보니 **그 이름의 인풋이 아예 존재하지 않았다**(vMix는 존재하지 않는 인풋 지정을 에러 없이 조용히 무시 — 이 프로젝트에 이미 여러 번 있었던 패턴). Master 앱 자체의 `vmixPgmTitle`은 낙관적으로 그 이름으로 먼저 표시되지만, 다음 폴링 틱에서 진짜 vMix 상태로 다시 맞춰지면서 마치 "누가 다른 곳에서 화면을 바꾼 것"처럼 보였던 것 — 실제로는 카메라 스위처 소스(cam1~4)들의 `vmixInput` 매핑이 지금 이 실제 vMix 인풋 구성과 어긋나 있는(오래돼 끊긴) 설정 문제였다. 테스트 종료 후 `liveSeq`는 원래 값으로 완전히 복원(서버에 저장된 실제 큐시트 파일은 curl로 재확인해 전혀 안 바뀜 확인)하고, PGM은 확인된 실제 인풋(`fullshot00237917.png`)으로 깨끗하게 정리 | **실제 vMix 라이브 시스템 + 실제 저장된 프로덕션 큐시트로 직접 검증.** 실제 GO/다음단계 버튼과 동일한 함수 경로, 실제 PGM 컷, 실제 재생 위치 실측(3회 재조회로 선형 증가 확인)까지 전부 실제 경로로 완료 — **최종 확인 완료.** ⚠️ 새로 발견된 별도 이슈(카메라 소스 cam1~4의 vmixInput이 현재 실제 vMix 인풋 구성과 어긋남, rs1~6 vmixInput 미설정)는 이번 수정 범위 밖이며 고치지 않음 — 사용자 확인·후속 조치 필요 |

| "기자 갈라치기"에 매칭해둔 소재가 안 나오던 진짜 근본 원인 — 재생만 됐지 "선택"이 없었음 (07-19) | 사용자 재보고: "지금도 출연과 중계에서 기자갈라치기 효과에 내가 설정한 소재가 나오지 않고 있는데.. 수정해줘". 지금까지 두 차례 수정(DVE 레이어 재생, 레이어 없는 서버 재생)은 전부 "그 서버에 지금 선택돼 있던 파일"을 Restart+Play하는 것까지만 했지, **어느 파일을 선택할지는 전혀 손대지 않았다** — 그런데 MARS엔 이미 07-13부터 "질문수→영상 슬롯"(`ensureClipSlotsForQuestions`) 기능이 있어 중계/출연 아이템의 `item.clips[질문번호]`에 사용자가 질문별로 정확히 그 소재 파일을 채워 넣게 돼 있었다(실제 저장된 아이템도 `clips:["world-item2.mov","world-item3.mov"]`처럼 questions 개수와 정확히 맞게 채워져 있었음) — Master의 LIVE 시퀀스 엔진이 이 값을 애초에 한 번도 읽지 않았던 것이 진짜 근본 원인이었다. `computeLivePhases()`의 SPLIT-S(서버 갈라치기) 단계에 질문 인덱스(`qIdx`)를 매기고, `advanceLiveSeq()`가 `item.clips[qIdx]`를 찾아 `playDveLayerServers()`에 넘기도록 수정 — 값이 있으면 이미 검증된 `_vmixArmFromList()`(SelectIndex+Restart+Play)로 정확히 그 파일을 선택해서 재생하고, 없으면(질문별 소재를 아직 안 채운 아이템) 기존처럼 "지금 선택된 것"을 재생하는 하위호환 유지. 사전 준비(`prearmLiveSeqPhaseDve`, PVW 프리뷰)에도 같은 파일을 미리 선택(재생은 안 함)해두도록 확장해, 온에어 전 PVW로 미리 봐도 실제로 나갈 그 파일이 보이게 함 | Playwright(vmixCall+vmixGetState 목킹, 질문 2개·서로 다른 clips 지정) — 질문1은 `SelectIndex&Value=1`(clipA.mov), 질문2는 `SelectIndex&Value=2`(clipB.mov)로 서로 다른 파일이 정확히 선택되는 것, 갈라치기가 아닌 단계(앵커/기자/FullShot)는 SelectIndex가 전혀 안 나가는 것 확인. **실제 vMix 라이브 시스템 직접 검증** — 유휴 SRV3(`미디어플레이어 예비`)에 실제로 존재하는 서로 다른 두 파일(`item1.mov`→`item2.mov`)을 순서대로 지정해 호출 → vMix가 응답하는 실제 `title` 속성이 매번 정확히 그 파일명으로 바뀌고 재생(`Running`, position 증가)되는 것까지 확인, PGM/PVW(active=17/preview=9) 불변 재확인 |

| 사용자 실사용 재현 — CUSTOM(단계 목록 직접 편집) 아이템은 소재 선택이 아예 안 먹었던 진짜 마지막 원인 (07-19) | 사용자가 브라우저 콘솔 덤프로 직접 제공한 실측 로그로 재현 — 테스트 중인 실제 아이템이 "단계 목록 직접 편집"(`liveSeq.phases`, `computeLivePhases()`의 `key:'CUSTOM'` 분기)을 쓰고 있었는데, 바로 앞의 수정은 질문수 자동생성 단계에만 붙는 `qIdx`로 소재를 찾았다 — CUSTOM 단계는 애초에 "몇 번째 질문"이라는 값이 없어(자유 순서 목록) 소재 선택 로직 자체가 전혀 안 걸렸고, 그 서버(미디어플레이어1)에 이전에 남아있던 클립을 그냥 재생하다 즉시 끝나버려(로그: "서버 클립 재생 종료 — 다음 리스트 항목으로 안 넘어가게 정지함") 화면이 안 바뀌는 것처럼 보였다. `qIdx` 전용 매칭을 버리고, "지금까지(이 단계 포함) 서버를 실제로 쓴 단계가 몇 번째인가"를 세는 `_liveSeqClipForPhase()`로 교체 — 자동생성/CUSTOM 어느 쪽이든 동일하게 적용되는 방식이라 이제 재발 여지가 없다 | Playwright — 사용자가 제공한 실제 콘솔 덤프(`liveSeq.phases` 4단계, `clips:["sport1.mov"]`)를 그대로 재구성해 vmixCall 목킹으로 재현: 이전엔(구 qIdx 로직) 소재 선택이 전혀 안 걸렸을 조합인데 수정 후 "서버 갈라치기" 단계에서 정확히 `SelectIndex`로 `sport1.mov`가 선택되는 것 확인. **실제 vMix 라이브 시스템 직접 검증** — DVE 레이어 경로(사용자의 실제 구성과 동일 — `dve*` 레이어 안에 `srv`가 들어있는 형태)로 유휴 SRV3에 실제 파일(`item1.mov`)을 지정해 호출 → vMix가 응답하는 실제 `title`이 정확히 그 파일로 바뀌고 재생되는 것, 정지 후 깨끗이 `Paused@0`로 남는 것까지 확인, PGM/PVW(사용자가 실제로 테스트하며 남겨둔 RS1/left-gala) 불변 |

| 디졸브 도중 멈춤 — "무조건 발생" 재보고, 이번엔 진짜 두 겹의 근본 원인을 찾음 (07-19) | 사용자 재보고: "이런 현상은 무조건 디졸브에서 발생하고 있어". 07-17~07-18에 이미 여러 차례 고쳤던(네트워크 왕복 유예·정밀 정지 루프 유예 등) 같은 계열의 증상인데 이번엔 근본적으로 다른 두 가지 원인이 겹쳐 있었다. **① server.py가 지금까지 단일 스레드(`HTTPServer`)로 요청을 하나씩만 처리**하고 있었는데, 실제로는 Master·MARS·cg-operator·자막담당 등 여러 PC/탭이 동시에 폴링 중이었다(실측: 서버 재시작 즉시 127.0.0.1/192.0.2.3/192.0.2.2 세 곳에서 동시에 `/api/cg-state` 요청 — 자막 오버레이가 250ms 주기로 폴링하는데 이게 3곳이면 초당 12회). 단일 스레드라 이 요청들이 전부 줄을 서고, 동시 12개 요청 실측 시 개별 요청이 최대 1.2초까지 지연됐다(단독이면 70ms) — 디졸브를 거는 `Function=Fade` 요청도 이 줄서기에 걸리면, `_vmixCut()`이 "요청을 보내는 시점" 기준으로 잡아둔 안전장치 유예시간이 실제로 vMix에 명령이 도달하기도 전에 만료될 수 있다. `ThreadingHTTPServer`로 전환(+ 동시에 여러 스레드가 건드리는 CG_STATE/CG_REQUESTS 등 전역 상태에 락 추가, VideohubClient는 애초에 스레드로 설계돼 있어 그대로 안전)해 동시 12개 요청 지연을 1.2초→0.29초로 줄였다. **② (진짜 마지막 조각) `_vmixPollTick()`이 "직전 PGM"(`prevPgmTitle`)을 vMix 실측 상태를 가져오는 비동기 fetch가 "끝난 뒤"에 읽고 있었다** — 그 fetch가 서버 지연 등으로 수백ms 걸리는 동안 다른 곳(`_vmixCut()`)이 동기적으로 `vmixPgmTitle`을 새 값으로 먼저 바꿔놓으면, fetch가 들고 있는 값은 그 cut 이전(stale) 스냅샷인데 방금 세팅된 새 값을 "직전 값"으로 잘못 캡처하는 역전이 발생 — "방금 PGM에 들어온 그 서버"를 "방금 PGM에서 나간 것"으로 거꾸로 오판해 막 재생을 시작한 소재를 그 자리에서 즉시 되감아 멈춰버렸다(실측 로그: "미디어플레이어 예비: PGM에서 내려가 처음으로 되감아 정지" — 그런데 그 순간에도 실제 vMix `active`는 계속 그 서버였음, 즉 "떠난 적이 없는데 떠났다고 오판"). `prevPgmTitle`을 fetch 시작 "전"에 캡처하도록 순서를 바꿔 이 역전 자체를 제거 — ①이 이 경합의 발생 확률(fetch가 오래 걸릴수록 경합 창이 넓어짐)을 낮추고 ②가 경합 자체를 근본적으로 없앤다, 둘 다 필요했다 | **실제 vMix 라이브 시스템 직접 검증(실제 저장된 프로덕션 서버·실제 다중 클라이언트 폴링 부하가 걸린 상태에서).** 수정 전: 유휴 서버에 실제 Mix 전환으로 재생 시작 → 250~500ms 안에 매번 `Paused@0`으로 튕김(반복 재현), 그 순간에도 실제 vMix `active`는 안 바뀐 채였음(로그로 "떠난 적 없는데 떠났다고 오판"한 것 직접 확인). 수정 후: 같은 절차 반복 → 3.75초간 `Running`으로 위치가 320→4300ms까지 끊김 없이 매끄럽게 증가, 자동 되감기 로그 전혀 안 뜸. 서버 스레드화도 별도로 동시요청 12개 벤치마크(1.2s→0.29s 지연 감소)로 확인. 테스트 종료 후 서버는 `Paused@0`로 정리, PGM/PVW는 실측값 그대로 두었다(실제 다른 세션이 그 상태를 만들어둔 것으로 보여 임의로 되돌리지 않음) |

| 사용자 실사용 재현 2건 — 백그라운드 탭 타이머 억제, MARS 갱신 반영 안 됨(설계상 수동) (07-19) | 사용자 보고: "중계 아이템이... 서버소재가 중간에 멈추었어" + "마스에서 출연의 영상소스를 바꾸어서 마스터에 전달했는데... 영상소재가 바뀌지 않고 있어". 조사 결과 서로 다른 두 원인. **②는 버그가 아니라 이미 있는 안전장치**: 서버(`/api/cuesheet`)엔 MARS가 보낸 새 소재가 실제로 정상 도착해 있었음 — `broadcastStartTs`가 설정된(방송 시작을 누른) 뒤에는 MARS 갱신이 자동 반영되지 않고 "MARS에서 큐시트가 갱신되었습니다 — 방송적용" 배너로 사람이 직접 눌러야만 반영되도록 원래 그렇게 설계돼 있음(온에어 중 확인 없이 내용이 바뀌는 걸 막기 위함) — 사용자에게 배너 확인/"방송적용" 클릭 또는 "📡 서버 불러오기"를 안내. **①은 실제 원인 확인**: 사용자가 중계 테스트 중 실제로 MARS 탭으로 옮겨갔다 왔음을 직접 확인 — 브라우저가 백그라운드(비활성) 탭의 `setInterval`/`setTimeout`을 강하게 억제하는데(크롬: 숨겨진 탭은 최소 1초 간격으로 묶임, 오래 숨겨지면 더 느려짐), `_vmixPollTick`(1.5초)·`_srvFastSafetyTick`(0.3초)·`scheduleServerClipAutoStop()`의 정밀 정지 루프(0.1초) 전부 이 억제의 영향을 받는 반면 vMix 자체는 탭 상태와 무관하게 실시간으로 계속 진행돼, 탭이 자는 동안 실제 상황과 이 탭이 마지막으로 알던 상태가 크게 벌어지고 돌아왔을 때 그 차이를 뒤늦게/애매한 타이밍에 처리하면서 방금까지 잘 나오던 서버 소재가 멈춘 것처럼 보일 수 있음 — `visibilitychange` 리스너를 추가해 탭이 다시 보이는 즉시 `_vmixPollTick()`/`_srvFastSafetyTick()`을 강제로 한 번 더 돌려, 억제된 타이머가 알아서 따라잡을 때까지 기다리지 않고 최대한 빨리 실제 상태로 재동기화하도록 함 | Playwright(vmixConnected 상태에서 `visibilitychange` 이벤트를 강제 발생시켜 `_vmixPollTick()`이 실제로 즉시 호출되는 것 함수 후킹으로 확인) — 백그라운드 탭 자체를 실제로 몇 분간 재현하는 것은 테스트 환경상 어려워 이 부분은 근본 완치가 아니라 재동기화 지연을 최소화하는 완화 조치임을 사용자에게 안내 필요 |

| "새 버전 있음" 알림 배너 — MARS/Master 공통 (07-19) | 사용자 요청: "이런 일이 없어야 하는데, 근본적인 이유는 무엇인거야" → "새 버전이 있으면 알려주는 장치를 만들어줘". 근본 원인: 브라우저는 페이지 로드 시 딱 한 번 JS를 읽어들이고, 그 뒤 파일이 서버에서 아무리 바뀌어도 스스로 알아채지 못한다 — MARS/Master는 방송 내내 켜두는 화면이라 이 문제가 특히 잘 드러나서, 오늘 하루에도 이미 고친 버그를 "아직도 안 고쳐졌다"고 재보고받는 일이 여러 번 있었다(실제로는 예전 탭을 계속 쓰고 있었던 것). 페이지 로드 시점에 자기 자신의 실제 빌드 시각(`/api/version`의 `master_mtime`/`mars_mtime`)을 기억해두고, 1분 주기 + 다른 탭에서 돌아올 때(`visibilitychange`)마다 서버의 최신 값과 비교해, 서버 쪽이 더 새로우면 빨간 배너("🔄 지금 새로고침" 버튼)로 알리는 기능을 양쪽 앱에 동일하게 추가 — 자동 새로고침은 하지 않음(온에어 중/입력 중 갑자기 새로고침되는 게 더 위험하므로, 사람이 직접 눌러야 함) | Playwright(실제 함수 호출) — 페이지 로드 시 기준 시각 저장 확인, `/api/version` 응답을 더 새로운 값으로 목킹해 배너가 뜨는 것, 같은 값이면 안 뜨는 것(오탐 없음), `visibilitychange` 경로로도 동일하게 감지되는 것, 닫기 버튼 동작까지 MARS·Master 양쪽에서 확인 |

| Master 활동 로그 서버 영속화 (07-19, 실촬영 중 요청) | 사용자 요청: "문제점을 확인하는데는 로그 패널이 필요할듯. 그리고 해당 로그의 보관관련해서는 네가 최적의 방법을 찾아봐" — 디졸브 멈춤을 서버 접속 로그(vMix Function 호출 기록)만으로 분석하다가, "그 호출을 왜 했는지" 설명하는 앱 자신의 판단(`log()` 메시지)이 DOM에만 있어 새로고침하면 사라지고 200줄 넘으면 유실된다는 한계에 부딪힘. `log()`가 화면에 찍는 것과 동시에 메모리 버퍼에 쌓아뒀다가 3초 간격으로 배치 전송(`POST /api/client-log`), server.py가 날짜별 파일(`client-logs/YYYY-MM-DD.log`)에 이어붙임 — 조회는 `GET /api/client-log?date=...`. 매 줄마다 요청을 보내지 않고 배치로 묶은 이유는 오늘 이미 이 서버가 여러 클라이언트의 잦은 폴링 부하를 겪은 걸 실측했기 때문. 탭이 숨겨지거나 닫힐 때는 `sendBeacon`으로 우선 시도해 배송을 보장하고, 전송 실패 시(서버 일시 다운 등) 다음 주기에 재시도하되 최근 500줄로 상한을 둠. 14일 지난 로그 파일은 서버 시작 시 1회 정리. **실제 촬영 중에 배포** — 사용자 승인 받아 server.py를 즉시 재시작(다운타임 1초 미만 확인) | Playwright(실제 함수 호출) — `log()` 호출 → 강제 flush → `GET /api/client-log`로 실제 파일에 저장된 것 확인, `?list=1`로 날짜 목록 조회 확인. 실제 방송 중 재시작이라 사용자에게 먼저 타이밍 확인 후 진행(1초 미만 다운타임) |

## 🔶 진행 중

| 영역 | 상태 | 남은 것 |
|---|---|---|
| 장비 확장 (Expansion_PRD) | Videohub Phase 1 완료 | VMU(영상믹서) → AMU(오디오믹서) 단계 |
| 제작 큐시트 (LIVE 시사교양) | MARS 저작 + 화면 리허설까지 | Master 실행 엔진 연동 (뉴스 TYPE_RULES 전용이라 미지원) |
| 뉴스 아이템 – 중계/출연 아이템 | 풀 시퀀스 + 출연 전용 템플릿까지 완료(07-13) — 기본형(소스선택 버그 수정·라우터 프리셋·프리라우팅) + newsformat.md §2.3 질문수(n) 기반 자동 진행 엔진, MARS 기사 "질문 수" 필드→아이템 전파, MARS 시퀀스 소스 사전 설정. 07-15: 질문수 자동생성은 유지하되, 실제 실행되는 단계 목록(`liveSeq.phases`)을 아이템 편집창에서 개별 추가/삭제/순서변경/소스변경 가능하게 확장(레거시 질문수 전용 아이템은 그대로 하위호환). 07-19: 서버 갈라치기 단계의 서버 소재 재생(DVE 레이어 안/레이어 없이 직접 둘 다) + 질문별 매칭 소재 선택(`item.clips[qIdx]`)까지 실장비로 최종 검증 완료 | 실제 DVE/Multi-View 화면분할(right-gala/left-gala류) 자체를 이 아이템들에 연결해 쓰는 실장비 검증은 아직(현재 저장된 실 아이템은 카메라·RS를 직접 씀, DVE 미사용) — cam1~4/rs1~6 vmixInput 재매핑이 선행돼야 함(위 진행 중 항목 참고) |
| HTML 자막 이용 및 UI 점검 | 자막담당 페이지 1차 완료(07-11) — `cg-operator.html`(입력+설정), server.py `/api/cg-request` 릴레이 큐, Master 폴링·실행. 07-18: GT 템플릿 라벨 동기화 완료(`/api/cg-labels`), CG 스튜디오(`cg-studio.html`)·커스텀 HTML 템플릿(`cg-custom-overlay.html`)까지 추가 | 실PC 2대 환경에서 실전 테스트 |
| 버그 수정 | 07-11 집중 스윕(12건) 이후 07-13 추가 발견분 + 07-17 세션 마감 자체 코드 리뷰(9건, 위 완료 표 참고)까지 정리 완료 | 다음 실장비 세션에서 발견되는 추가분 |
| 자막 설정 — GT 인풋/필드 매핑 (07-18) | 사용자 확인 받아 5종 매핑 완료: 제목자막(lower→`bokdea.gtzip`/`TextBlock1`), 이름자막(name→`namebokdea.gtzip`/`TextBlock1`), 티커(ticker→`Ticker.gtzip`/`Ticker1`), 하단자막(bottom→`mbc-low.gtzip`/`TextBlock1`), 시간자막(time→`mbc-high.gtzip`/`TextBlock2`) | 나머지(긴급자막/위치자막/코너자막)는 여전히 기본값(`GT타이틀1`)인 채 — 실제 vMix엔 `Text.gtzip`(TextBlock2 필드)이 아직 안 쓰였고, `mbc-hand.gtzip`은 텍스트 필드가 아니라 이미지 필드(`Image1.Source`)라 자막용으로는 쓸 수 없음 — 어느 자막 종류에 `Text.gtzip`을 쓸지 사용자 확인 필요, 긴급/위치/코너자막은 매칭되는 실제 템플릿이 아직 없어 새로 vMix에 만들어야 할 수 있음 |
| 카메라(cam1~4)·RS1~6 스위처 소스의 vmixInput이 실제 vMix 인풋 구성과 어긋남 (07-19 발견) | 서버 소재 재생 버그 최종 확인 중 발견 — `cam2`의 저장된 값 `GT-title1.gtzip`으로 실제 컷을 걸었는데 지금 실제 vMix(192.0.2.2)의 인풋 23개 중 그 이름을 가진 인풋이 아예 없었다(vMix는 존재하지 않는 인풋 지정을 에러 없이 조용히 무시). `rs1`은 아예 `vmixInput`이 빈 값. 실제 카메라 하드웨어가 없어 대체 인풋으로 자리만 매핑해둔 상태([[real-vmix-environment]])인데, 그 대체 인풋 자체가 언젠가 vMix 쪽에서 이름이 바뀌었거나 삭제된 것으로 추정 — 앱 버그가 아니라 설정 드리프트 | 설정 탭 "스위처 소스 관리"에서 cam1~4·rs1~6의 vmixInput을 지금 실제 vMix 인풋 목록에 맞게 다시 채워야 함(사용자 확인 필요) |
| 자동 샷전환 — 오디오 레벨 기반 (07-23 착수) | Master 본체 기능으로 1차 구현 완료(별도 도구 아님, 사용자 명시 요청) — 스위처 탭에 실시간 레벨 미터+모드(수동/반자동/자동)+GO 수동 오버라이드 패널, PDF/설정 탭에 카메라별 마이크 매핑·임계값·유지시간·쿨다운·전환효과 설정. 전환 명령은 새로 만들지 않고 기존 `_vmixCut()` 재사용, 판정 루프는 `_vmixPollTick`과 별개의 독립 타이머(`_startVMixPoll`/`_stopVMixPoll`에 연결). vMix 연결 끊김 감지 시 자동으로 수동 모드 폴백(재연결해도 자동 복귀는 안 함 — 의도적 안전장치). 근거: `spec/vMix_자동샷전환_PRD.md` | **실장비 미검증** — vMix XML 상태에서 오디오 진폭을 읽는 필드명(`meterF1` 등 후보 여러 개 시도)이 실제 vMix 응답과 맞는지 확인 전. 카메라-마이크 매핑도 아직 미입력(사용자 마이크 준비 후 입력 예정) |
| 오디오 기사 하이라이트 — 독립 도구 (07-23) | `audio-highlight/`(저장소 루트, Master와 별도 프로세스/포트) — 실제 사용자 피드백 반영해 반복 개선 완료. ① 판정 로직 변경: 유사도가 낮아도 위치는 항상 진행(더 이상 "매칭실패→정지" 없음), 기준(`min_score`, 기본 35) 미만이면 초록 대신 노란색 "저신뢰" 표시만 — 사용자가 "중요한 건 어디를 진행하느냐"라고 지적해 반영. ② 민감도(`min_score`)·탐색범위(`forward_window`) 서버 재시작 없이 GUI 슬라이더로 실시간 조절(`/api/settings`). ③ 브라우저에서 기사 원문 textarea 붙여넣기 + 오디오 파일 업로드로 서버 재시작·모델 재로딩 없이 반복 재테스트(`/api/article`, `/api/upload-audio`, 재생 세대 번호로 이전 업로드와 경쟁 방지). 전부 실제 서버 기동+Playwright+실TTS 오디오로 종단 검증(다른 기사 2종으로 교차 테스트 포함). 실사용자(갤럭시, 이 Mac은 물리 마이크 없이 NDI Audio만 있음)는 폰 녹음 파일 업로드로 테스트하는 경로로 확정. `python`/`pip` PATH 문제는 `~/.local/bin` 심볼릭 링크로 해결. **Master 통합은 사용자 요청으로 보류**. 같은 날 추가 세션(사용자 재요청으로 재착수): ④ "새 기사 원문"에 텍스트만 넣어도(오디오 없이) 즉시 등록·미리보기되도록 버튼 로직 분리 — 오디오 파일을 먼저 골라야만 등록 자체가 되던 게 "다른 기사를 입력해도 반영이 안 된다"는 원인이었음(실사용자 보고). 기사 표시 영역(`main`)을 헤더/설정 바와 분리된 자체 스크롤 패널로 바꿔 긴 기사도 끝까지 스크롤로 확인 가능. ⑤ **종료 임박 경고 신설** — wav 재생 모드에서 파일 총 길이 대비 남은 시간을 매 폴링마다 재계산해, 설정한 시간(기본 20초) 이하로 남으면 "아직 안 읽은 문장(현재 위치~끝)"에만 빨간 배경이 2초 주기로 점멸(처음엔 화면 전체 오버레이로 만들었다가 "화면 전체 말고 기사 영역만" → "기사 영역 전체 말고 남은 내용만"으로 사용자 피드백 2회 반영해 범위를 좁혔음). 켜기/끄기·N초 임계값 모두 GUI 실시간 조절(`/api/settings`에 `warn_enabled`/`warn_before_sec` 추가) + CLI(`--warn-before-sec`/`--no-warn`). 마이크 모드는 총 길이를 알 수 없어 항상 비활성(의도된 동작). ⑥ **마이크 실사용 준비** — `--list-devices`(입력 장치 목록+시스템 기본값 표시)/`--input-device`(번호 또는 이름 일부로 장치 지정) 추가, 화면 상태줄에 지금 캡처 중인 장치명 표시, README의 "브라우저가 마이크 권한을 요청한다"는 잘못된 서술을 "서버가 직접 sounddevice로 캡처"로 정정. 근거: `spec/오디오_기사_하이라이트_PRD.md` | 이 Mac은 여전히 물리 마이크가 없어(NDI Audio만 입력 장치로 잡힘) 마이크 모드 자체는 코드 준비만 끝났고 실제 캡처는 미검증 — 실제 마이크 연결 후 사람 목소리로 최초 실측 및 지연 측정 → Master 편입 여부 결정 |

## ⬜ 미착수 (설계 문서만 존재)

| 영역 | 문서 | 비고 |
|---|---|---|
| 로그인/계정 | spec/login.md | 코드 0%. **선행 결정 필요**: 사업 모델(온프레미스/SaaS/하이브리드) + 백엔드 스택(기존 PRD의 Node+PostgreSQL vs login.md의 Next.js+Supabase) 정리 |
| 뉴스 형식 표준화 | spec/newsformat.md | MOS Protocol/NRCS 연동 데이터 모델 — 설계 단계 |
| Phase 2 백엔드 | spec/NEWS_BROADCAST_PRD.md | 멀티유저·권한·DB — 여전히 Phase 1(JSON 파일) |
| 결제/구독/멀티테넌시 | (없음) | 상업화 시 별도 기획 필요 |
| 실카메라 연동 | — | 하드웨어 미보유. 현재 카메라 슬롯은 대체 인풋으로 자리만 매핑 |

## 📅 단기 일정 (→ 2026-07-17 중간보고)

1. **(최우선)** 사업 모델 + 백엔드 스택 결정 → login.md 개정
2. Supabase 프로젝트 + Next.js 스캐폴딩 (기존 정적 HTML과의 공존 방식 포함)
3. 로컬 계정 1단계 구현 + login.md 2.5 체크리스트 5건 수동 통과
4. 구글 OAuth 앱 등록 선착수(심사 대기), 카카오 비즈앱 여부·네이버 raw API 사전 확인
5. 이 문서 갱신 + 중간보고

> 소셜 로그인 3종 완성은 이번 주 목표가 아님 — "로컬 계정 실동작 + 방향 결정"이 보고의 핵심.

---

**갱신 규칙**: 큰 기능이 완료/착수될 때 이 문서의 해당 행을 옮기고 기준일을 갱신한다. 세부 이력은 git log와 README 변경 이력이 담당하고, 이 문서는 "현재 스냅샷"만 유지한다.
